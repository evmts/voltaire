---
title: How to Get Current Block Number
description: Query the latest block number from an Ethereum node
---

# Get Current Block Number

Use `eth_blockNumber` to get the most recent block.

```typescript
import { createJsonRpcProvider } from '@tevm/voltaire/jsonrpc';
import * as Rpc from '@tevm/voltaire/jsonrpc';

const provider = createJsonRpcProvider('https://eth.llamarpc.com');

const blockNumber = await provider.request(Rpc.Eth.BlockNumberRequest());

console.log('Current block:', blockNumber);
// '0x112a880' (hex string)

// Convert to number
const blockNum = parseInt(blockNumber, 16);
console.log('Block number:', blockNum);
// 18016384
```

## Related

- [Block Methods](/jsonrpc-provider/eth-methods/blocks) - All block query methods
- [Getting Started](/jsonrpc-provider/getting-started) - Provider setup
