---
title: '[TS/JS] docs/primitives/bytecode/equals.test.ts'
source: 'docs/primitives/bytecode/equals.test.ts'
---

> Auto-generated from test file: docs/primitives/bytecode/equals.test.ts

```typescript
import { describe, expect, it } from "vitest";

describe("Bytecode.equals (docs/primitives/bytecode/equals.mdx)", () => {
	describe("Basic Equality", () => {
		it("should return true for identical bytecode", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			const code1 = Bytecode("0x600100");
			const code2 = Bytecode("0x600100");

			expect(code1.equals(code2)).toBe(true);
		});

		it("should return false for different bytecode", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			const code1 = Bytecode("0x600100");
			const code2 = Bytecode("0x600200");

			expect(code1.equals(code2)).toBe(false);
		});

		it("should return false for different lengths", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			const code1 = Bytecode("0x600100");
			const code2 = Bytecode("0x60010000");

			expect(code1.equals(code2)).toBe(false);
		});
	});

	describe("Edge Cases", () => {
		it("should compare empty bytecode", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			const code1 = Bytecode("0x");
			const code2 = Bytecode("0x");

			expect(code1.equals(code2)).toBe(true);
		});

		it("should compare self", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			const code = Bytecode("0x600100");

			expect(code.equals(code)).toBe(true);
		});
	});

	describe("Contract Comparison", () => {
		it("should detect identical contracts from different sources", async () => {
			const { Bytecode } = await import(
				"../../../src/primitives/Bytecode/index.js"
			);

			// Same bytecode created differently
			const fromHex = Bytecode.fromHex("0x6080604052");
			const fromArray = Bytecode(new Uint8Array([0x60, 0x80, 0x60, 0x40, 0x52]));

			expect(fromHex.equals(fromArray)).toBe(true);
		});
	});
});

```
