---
title: '[TS/JS] docs/crypto/ed25519/index.test.ts'
source: 'docs/crypto/ed25519/index.test.ts'
---

> Auto-generated from test file: docs/crypto/ed25519/index.test.ts

```typescript
/**
 * Tests for docs/crypto/ed25519/index.mdx code examples
 *
 * API DISCREPANCIES FOUND:
 * 1. Doc uses `import * as Ed25519 from '@tevm/voltaire/Ed25519'` but actual import is `import { Ed25519 } from '../../../src/crypto/Ed25519/index.js'`
 * 2. Doc shows `Hex('0x...')` returning a Uint8Array directly - actual API returns HexType (branded string).
 *    Must use `Hex.toBytes(Hex('0x...'))` to get Uint8Array for Ed25519 functions.
 * 3. Doc code examples use Hex directly as Uint8Array which is incorrect - Hex is a string type
 */

import { describe, expect, it } from "vitest";

describe("docs/crypto/ed25519/index.mdx", () => {
	describe("Quick Start example", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);
		const { Hex } = await import("../../../src/primitives/Hex/index.js");

		it("generates keypair from seed, signs, and verifies", () => {
			// Generate keypair from seed
			// Doc uses: const seed = Hex('0x9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60');
			// API DISCREPANCY: Hex returns HexType (string), need Hex.toBytes() to convert to Uint8Array
			const seed = Hex.toBytes(
				Hex("0x9d61b19deffd5a60ba844af492ec2cc44449c5697b326919703bac031cae7f60"),
			);
			const keypair = Ed25519.keypairFromSeed(seed);

			// Sign a message (any length)
			const message = new TextEncoder().encode("Hello, Ed25519!");
			const signature = Ed25519.sign(message, keypair.secretKey);

			// Verify signature
			const isValid = Ed25519.verify(
				signature,
				message,
				keypair.publicKey,
			);
			expect(isValid).toBe(true);
		});
	});

	describe("Key Generation - keypairFromSeed", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);
		const { Hex } = await import("../../../src/primitives/Hex/index.js");

		it("generates keypair from 32-byte seed", () => {
			// API DISCREPANCY: Doc shows Hex() returning Uint8Array, but it returns string
			const seed = Hex.toBytes(
				Hex("0x1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef"),
			);
			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed);

			expect(secretKey).toBeInstanceOf(Uint8Array);
			expect(secretKey.length).toBe(32);
			expect(publicKey).toBeInstanceOf(Uint8Array);
			expect(publicKey.length).toBe(32);
		});
	});

	describe("Key Generation - derivePublicKey", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		it("derives public key from secret key", () => {
			const seed = new Uint8Array(32).fill(1);
			const keypair = Ed25519.keypairFromSeed(seed);
			const publicKey = Ed25519.derivePublicKey(keypair.secretKey);

			expect(publicKey).toEqual(keypair.publicKey);
		});
	});

	describe("Signing - sign", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		it("signs message and produces 64-byte signature", () => {
			const seed = new Uint8Array(32).fill(1);
			const keypair = Ed25519.keypairFromSeed(seed);

			const message = new TextEncoder().encode("Sign this message");
			const signature = Ed25519.sign(message, keypair.secretKey);

			expect(signature.length).toBe(64);
		});
	});

	describe("Verification - verify", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		it("verifies valid signature returns true", () => {
			const seed = new Uint8Array(32).fill(1);
			const keypair = Ed25519.keypairFromSeed(seed);
			const message = new TextEncoder().encode("test");
			const signature = Ed25519.sign(message, keypair.secretKey);

			const valid = Ed25519.verify(signature, message, keypair.publicKey);
			expect(valid).toBe(true);
		});
	});

	describe("Validation functions", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		// API DISCREPANCY: Doc shows these returning boolean,
		// but actual API returns boolean (true/false)

		it("validateSecretKey returns boolean", () => {
			const secretKey = new Uint8Array(32).fill(1);
			const result = Ed25519.validateSecretKey(secretKey);
			expect(typeof result).toBe("boolean");
			expect(result).toBe(true);
		});

		it("validatePublicKey returns boolean", () => {
			const seed = new Uint8Array(32).fill(1);
			const keypair = Ed25519.keypairFromSeed(seed);
			const result = Ed25519.validatePublicKey(keypair.publicKey);
			expect(typeof result).toBe("boolean");
			expect(result).toBe(true);
		});

		it("validateSeed returns boolean", () => {
			const seed = new Uint8Array(32).fill(1);
			const result = Ed25519.validateSeed(seed);
			expect(typeof result).toBe("boolean");
			expect(result).toBe(true);
		});

		it("validateSecretKey rejects wrong length", () => {
			const invalidSecretKey = new Uint8Array(16);
			const result = Ed25519.validateSecretKey(invalidSecretKey);
			expect(result).toBe(false);
		});

		it("validatePublicKey rejects wrong length", () => {
			const invalidPublicKey = new Uint8Array(16);
			const result = Ed25519.validatePublicKey(invalidPublicKey);
			expect(result).toBe(false);
		});

		it("validateSeed rejects wrong length", () => {
			const invalidSeed = new Uint8Array(16);
			const result = Ed25519.validateSeed(invalidSeed);
			expect(result).toBe(false);
		});
	});

	describe("Constants", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		it("has correct constant values", () => {
			expect(Ed25519.SECRET_KEY_SIZE).toBe(32);
			expect(Ed25519.PUBLIC_KEY_SIZE).toBe(32);
			expect(Ed25519.SIGNATURE_SIZE).toBe(64);
			expect(Ed25519.SEED_SIZE).toBe(32);
		});
	});

	describe("RFC 8032 Test Vectors", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);

		it("verifies RFC 8032 test vector 1 from docs", () => {
			// Test vector 1 from RFC 8032 (as shown in docs)
			const seed1 = new Uint8Array([
				0x9d, 0x61, 0xb1, 0x9d, 0xef, 0xfd, 0x5a, 0x60, 0xba, 0x84,
				0x4a, 0xf4, 0x92, 0xec, 0x2c, 0xc4, 0x44, 0x49, 0xc5, 0x69,
				0x7b, 0x32, 0x69, 0x19, 0x70, 0x3b, 0xac, 0x03, 0x1c, 0xae,
				0x7f, 0x60,
			]);

			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed1);

			// Expected public key
			const expectedPublicKey = new Uint8Array([
				0xd7, 0x5a, 0x98, 0x01, 0x82, 0xb1, 0x0a, 0xb7, 0xd5, 0x4b,
				0xfe, 0xd3, 0xc9, 0x64, 0x07, 0x3a, 0x0e, 0xe1, 0x72, 0xf3,
				0xda, 0xa6, 0x23, 0x25, 0xaf, 0x02, 0x1a, 0x68, 0xf7, 0x07,
				0x51, 0x1a,
			]);

			expect(
				publicKey.every((byte, i) => byte === expectedPublicKey[i]),
			).toBe(true);

			// Sign empty message
			const message = new Uint8Array(0);
			const signature = Ed25519.sign(message, secretKey);

			// Expected signature
			const expectedSignature = new Uint8Array([
				0xe5, 0x56, 0x43, 0x00, 0xc3, 0x60, 0xac, 0x72, 0x90, 0x86,
				0xe2, 0xcc, 0x80, 0x6e, 0x82, 0x8a, 0x84, 0x87, 0x7f, 0x1e,
				0xb8, 0xe5, 0xd9, 0x74, 0xd8, 0x73, 0xe0, 0x65, 0x22, 0x49,
				0x01, 0x55, 0x5f, 0xb8, 0x82, 0x15, 0x90, 0xa3, 0x3b, 0xac,
				0xc6, 0x1e, 0x39, 0x70, 0x1c, 0xf9, 0xb4, 0x6b, 0xd2, 0x5b,
				0xf5, 0xf0, 0x59, 0x5b, 0xbe, 0x24, 0x65, 0x51, 0x41, 0x43,
				0x8e, 0x7a, 0x10, 0x0b,
			]);

			expect(
				signature.every((byte, i) => byte === expectedSignature[i]),
			).toBe(true);

			// Verify signature
			expect(Ed25519.verify(signature, message, publicKey)).toBe(true);
		});
	});

	describe("Deterministic Signatures", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);
		const { Hex } = await import("../../../src/primitives/Hex/index.js");

		it("produces identical signatures for same message and key", () => {
			// Ed25519 is deterministic - same message + key = same signature
			// API DISCREPANCY: Doc shows Hex() returning Uint8Array, but it returns string
			const seed = Hex.toBytes(
				Hex("0xabcdef1234567890abcdef1234567890abcdef1234567890abcdef1234567890"),
			);
			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed);

			const message = new TextEncoder().encode("test");

			const sig1 = Ed25519.sign(message, secretKey);
			const sig2 = Ed25519.sign(message, secretKey);

			// Identical signatures
			expect(sig1.every((byte, i) => byte === sig2[i])).toBe(true);
		});
	});

	describe("Message Length Flexibility", async () => {
		const { Ed25519 } = await import(
			"../../../src/crypto/Ed25519/index.js"
		);
		const { Hex } = await import("../../../src/primitives/Hex/index.js");

		it("signs and verifies empty message", () => {
			// API DISCREPANCY: Doc shows Hex() returning Uint8Array, but it returns string
			const seed = Hex.toBytes(
				Hex("0xfedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321"),
			);
			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed);

			// Empty message
			// Doc uses: const emptyMsg = Hex('0x');
			// API DISCREPANCY: Hex('0x') returns empty string, need Hex.toBytes for empty Uint8Array
			const emptyMsg = new Uint8Array(0);
			const sig0 = Ed25519.sign(emptyMsg, secretKey);
			expect(Ed25519.verify(sig0, emptyMsg, publicKey)).toBe(true);
		});

		it("signs and verifies short message", () => {
			const seed = Hex.toBytes(
				Hex("0xfedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321"),
			);
			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed);

			// Short message
			const msg1 = new TextEncoder().encode("Hi");
			const sig1 = Ed25519.sign(msg1, secretKey);
			expect(Ed25519.verify(sig1, msg1, publicKey)).toBe(true);
		});

		it("signs and verifies large message (1 MB)", () => {
			const seed = Hex.toBytes(
				Hex("0xfedcba0987654321fedcba0987654321fedcba0987654321fedcba0987654321"),
			);
			const { secretKey, publicKey } = Ed25519.keypairFromSeed(seed);

			// Long message (1 MB)
			const largeData = new TextEncoder().encode("a".repeat(1024 * 1024));
			const sig2 = Ed25519.sign(largeData, secretKey);
			expect(Ed25519.verify(sig2, largeData, publicKey)).toBe(true);
		});
	});
});

```
