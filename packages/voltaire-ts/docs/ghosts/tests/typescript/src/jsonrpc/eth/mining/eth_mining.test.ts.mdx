---
title: '[TS/JS] src/jsonrpc/eth/mining/eth_mining.test.ts'
source: 'src/jsonrpc/eth/mining/eth_mining.test.ts'
---

> Auto-generated from test file: src/jsonrpc/eth/mining/eth_mining.test.ts

```typescript
import { describe, expect, test } from "vitest";
import { MiningRequest, method } from "./eth_mining.js";

describe("eth_mining", () => {
	describe("Request Creation", () => {
		test("creates request with no parameters", () => {
			const req = MiningRequest();
			expect(req).toEqual({
				method: "eth_mining",
			});
		});

		test("method constant is correct", () => {
			expect(method).toBe("eth_mining");
		});
	});

	describe("Request Structure", () => {
		test("returns RequestArguments type", () => {
			const req = MiningRequest();
			expect(req).toHaveProperty("method");
			expect(req.params).toBeUndefined();
		});

		test("method matches constant", () => {
			const req = MiningRequest();
			expect(req.method).toBe(method);
		});
	});
});

```
