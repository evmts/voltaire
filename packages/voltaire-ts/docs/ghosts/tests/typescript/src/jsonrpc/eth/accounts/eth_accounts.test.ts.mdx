---
title: '[TS/JS] src/jsonrpc/eth/accounts/eth_accounts.test.ts'
source: 'src/jsonrpc/eth/accounts/eth_accounts.test.ts'
---

> Auto-generated from test file: src/jsonrpc/eth/accounts/eth_accounts.test.ts

```typescript
import { describe, expect, test } from "vitest";
import { AccountsRequest, method } from "./eth_accounts.js";

describe("eth_accounts", () => {
	describe("Request Creation", () => {
		test("creates request with no parameters", () => {
			const req = AccountsRequest();
			expect(req).toEqual({
				method: "eth_accounts",
			});
		});

		test("method constant is correct", () => {
			expect(method).toBe("eth_accounts");
		});
	});

	describe("Request Structure", () => {
		test("returns RequestArguments type", () => {
			const req = AccountsRequest();
			expect(req).toHaveProperty("method");
			expect(req.params).toBeUndefined();
		});

		test("method matches constant", () => {
			const req = AccountsRequest();
			expect(req.method).toBe(method);
		});
	});
});

```
