---
title: '[TS/JS] src/primitives/Slot/equals.test.ts'
source: 'src/primitives/Slot/equals.test.ts'
---

> Auto-generated from test file: src/primitives/Slot/equals.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as Slot from "./index.js";

describe("Slot.equals", () => {
	describe("equal slots", () => {
		it("returns true for same value", () => {
			const a = Slot.from(1000000n);
			const b = Slot.from(1000000n);
			expect(Slot.equals(a, b)).toBe(true);
		});

		it("returns true for zero slots", () => {
			const a = Slot.from(0);
			const b = Slot.from(0);
			expect(Slot.equals(a, b)).toBe(true);
		});

		it("returns true for same value from different input types", () => {
			const a = Slot.from(42);
			const b = Slot.from(42n);
			expect(Slot.equals(a, b)).toBe(true);
		});

		it("returns true for same value from string", () => {
			const a = Slot.from("1000");
			const b = Slot.from(1000);
			expect(Slot.equals(a, b)).toBe(true);
		});

		it("returns true for large equal values", () => {
			const large = 2n ** 100n;
			const a = Slot.from(large);
			const b = Slot.from(large);
			expect(Slot.equals(a, b)).toBe(true);
		});
	});

	describe("different slots", () => {
		it("returns false for different values", () => {
			const a = Slot.from(1000000n);
			const b = Slot.from(1000001n);
			expect(Slot.equals(a, b)).toBe(false);
		});

		it("returns false for zero vs non-zero", () => {
			const a = Slot.from(0);
			const b = Slot.from(1);
			expect(Slot.equals(a, b)).toBe(false);
		});

		it("returns false for off-by-one", () => {
			const a = Slot.from(100);
			const b = Slot.from(101);
			expect(Slot.equals(a, b)).toBe(false);
		});

		it("returns false for very different values", () => {
			const a = Slot.from(1);
			const b = Slot.from(1000000);
			expect(Slot.equals(a, b)).toBe(false);
		});
	});

	describe("edge cases", () => {
		it("handles epoch boundary slots", () => {
			const a = Slot.from(31);
			const b = Slot.from(32);
			expect(Slot.equals(a, b)).toBe(false);
		});

		it("handles same epoch boundary", () => {
			const a = Slot.from(32);
			const b = Slot.from(32);
			expect(Slot.equals(a, b)).toBe(true);
		});
	});
});

```
