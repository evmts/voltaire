---
title: '[TS/JS] src/primitives/Abi/error/standards/ERC721Errors.test.ts'
source: 'src/primitives/Abi/error/standards/ERC721Errors.test.ts'
---

> Auto-generated from test file: src/primitives/Abi/error/standards/ERC721Errors.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { keccak256String } from "../../../Hash/BrandedHashIndex.js";
import * as Selector from "../../../Selector/index.js";
import { GetSelector } from "../getSelector.js";
import * as ERC721 from "./ERC721Errors.js";

const getSelector = GetSelector({ keccak256String });

describe("ERC721Errors", () => {
	it("should have correct selector for ERC721InvalidOwner", () => {
		const selector = getSelector(ERC721.ERC721InvalidOwner);
		const expected = Selector.fromSignature("ERC721InvalidOwner(address)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721NonexistentToken", () => {
		const selector = getSelector(ERC721.ERC721NonexistentToken);
		const expected = Selector.fromSignature("ERC721NonexistentToken(uint256)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721IncorrectOwner", () => {
		const selector = getSelector(ERC721.ERC721IncorrectOwner);
		const expected = Selector.fromSignature(
			"ERC721IncorrectOwner(address,uint256,address)",
		);
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721InvalidSender", () => {
		const selector = getSelector(ERC721.ERC721InvalidSender);
		const expected = Selector.fromSignature("ERC721InvalidSender(address)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721InvalidReceiver", () => {
		const selector = getSelector(ERC721.ERC721InvalidReceiver);
		const expected = Selector.fromSignature("ERC721InvalidReceiver(address)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721InsufficientApproval", () => {
		const selector = getSelector(ERC721.ERC721InsufficientApproval);
		const expected = Selector.fromSignature(
			"ERC721InsufficientApproval(address,uint256)",
		);
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721InvalidApprover", () => {
		const selector = getSelector(ERC721.ERC721InvalidApprover);
		const expected = Selector.fromSignature("ERC721InvalidApprover(address)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct selector for ERC721InvalidOperator", () => {
		const selector = getSelector(ERC721.ERC721InvalidOperator);
		const expected = Selector.fromSignature("ERC721InvalidOperator(address)");
		expect(Selector.toHex(selector)).toBe(Selector.toHex(expected));
	});

	it("should have correct error structure", () => {
		expect(ERC721.ERC721IncorrectOwner).toEqual({
			type: "error",
			name: "ERC721IncorrectOwner",
			inputs: [
				{ name: "sender", type: "address" },
				{ name: "tokenId", type: "uint256" },
				{ name: "owner", type: "address" },
			],
		});
	});
});

```
