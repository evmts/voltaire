---
title: '[TS/JS] src/primitives/Address/errors.test.ts'
source: 'src/primitives/Address/errors.test.ts'
---

> Auto-generated from test file: src/primitives/Address/errors.test.ts

```typescript
import { describe, expect, it } from "vitest";
import {
	InvalidAddressLengthError,
	InvalidHexFormatError,
	InvalidHexStringError,
	InvalidValueError,
	NotImplementedError,
} from "./errors.js";

describe("InvalidHexFormatError", () => {
	it("creates error with default message", () => {
		const error = new InvalidHexFormatError();
		expect(error.message).toContain("Invalid hex format for address");
		expect(error.name).toBe("InvalidHexFormatError");
	});

	it("creates error with custom message", () => {
		const error = new InvalidHexFormatError("Custom message");
		expect(error.message).toContain("Custom message");
	});

	it("sets error code", () => {
		const error = new InvalidHexFormatError();
		expect(error.code).toBe("INVALID_HEX_FORMAT");
	});

	it("sets custom error code", () => {
		const error = new InvalidHexFormatError(undefined, {
			code: "CUSTOM_CODE",
		});
		expect(error.code).toBe("CUSTOM_CODE");
	});

	it("stores invalid value", () => {
		const error = new InvalidHexFormatError(undefined, {
			value: "invalid",
		});
		expect(error.value).toBe("invalid");
	});

	it("sets expected format", () => {
		const error = new InvalidHexFormatError();
		expect(error.expected).toBe("0x-prefixed hex string");
	});

	it("sets custom expected format", () => {
		const error = new InvalidHexFormatError(undefined, {
			expected: "custom format",
		});
		expect(error.expected).toBe("custom format");
	});

	it("sets docs path", () => {
		const error = new InvalidHexFormatError();
		expect(error.docsPath).toBe(
			"/primitives/address/fundamentals#error-handling",
		);
	});

	it("sets custom docs path", () => {
		const error = new InvalidHexFormatError(undefined, {
			docsPath: "/custom/path",
		});
		expect(error.docsPath).toBe("/custom/path");
	});

	it("stores context", () => {
		const error = new InvalidHexFormatError(undefined, {
			context: { key: "value" },
		});
		expect(error.context).toEqual({ key: "value" });
	});

	it("stores cause", () => {
		const cause = new Error("root cause");
		const error = new InvalidHexFormatError(undefined, { cause });
		expect(error.cause).toBe(cause);
	});
});

describe("InvalidHexStringError", () => {
	it("creates error with default message", () => {
		const error = new InvalidHexStringError();
		expect(error.message).toContain("Invalid hex string");
		expect(error.name).toBe("InvalidHexStringError");
	});

	it("creates error with custom message", () => {
		const error = new InvalidHexStringError("Custom message");
		expect(error.message).toContain("Custom message");
	});

	it("sets error code", () => {
		const error = new InvalidHexStringError();
		expect(error.code).toBe("INVALID_HEX_STRING");
	});

	it("sets custom error code", () => {
		const error = new InvalidHexStringError(undefined, {
			code: "CUSTOM_CODE",
		});
		expect(error.code).toBe("CUSTOM_CODE");
	});

	it("stores invalid value", () => {
		const error = new InvalidHexStringError(undefined, {
			value: "0xGGG",
		});
		expect(error.value).toBe("0xGGG");
	});

	it("sets expected format", () => {
		const error = new InvalidHexStringError();
		expect(error.expected).toBe("Valid hexadecimal characters (0-9, a-f, A-F)");
	});

	it("sets custom expected format", () => {
		const error = new InvalidHexStringError(undefined, {
			expected: "custom format",
		});
		expect(error.expected).toBe("custom format");
	});

	it("sets docs path", () => {
		const error = new InvalidHexStringError();
		expect(error.docsPath).toBe(
			"/primitives/address/fundamentals#error-handling",
		);
	});

	it("stores context", () => {
		const error = new InvalidHexStringError(undefined, {
			context: { position: 5 },
		});
		expect(error.context).toEqual({ position: 5 });
	});

	it("stores cause", () => {
		const cause = new Error("root cause");
		const error = new InvalidHexStringError(undefined, { cause });
		expect(error.cause).toBe(cause);
	});
});

describe("InvalidAddressLengthError", () => {
	it("creates error with default message", () => {
		const error = new InvalidAddressLengthError();
		expect(error.message).toContain("Invalid address length");
		expect(error.name).toBe("InvalidAddressLengthError");
	});

	it("creates error with custom message", () => {
		const error = new InvalidAddressLengthError("Custom message");
		expect(error.message).toContain("Custom message");
	});

	it("sets error code", () => {
		const error = new InvalidAddressLengthError();
		expect(error.code).toBe("INVALID_ADDRESS_LENGTH");
	});

	it("sets custom error code", () => {
		const error = new InvalidAddressLengthError(undefined, {
			code: "CUSTOM_CODE",
		});
		expect(error.code).toBe("CUSTOM_CODE");
	});

	it("stores invalid value", () => {
		const value = new Uint8Array([1, 2, 3]);
		const error = new InvalidAddressLengthError(undefined, {
			value,
		});
		expect(error.value).toBe(value);
	});

	it("sets expected length", () => {
		const error = new InvalidAddressLengthError();
		expect(error.expected).toBe("20 bytes");
	});

	it("sets custom expected length", () => {
		const error = new InvalidAddressLengthError(undefined, {
			expected: "32 bytes",
		});
		expect(error.expected).toBe("32 bytes");
	});

	it("sets docs path", () => {
		const error = new InvalidAddressLengthError();
		expect(error.docsPath).toBe(
			"/primitives/address/fundamentals#error-handling",
		);
	});

	it("stores context", () => {
		const error = new InvalidAddressLengthError(undefined, {
			context: { actualLength: 32 },
		});
		expect(error.context).toEqual({ actualLength: 32 });
	});

	it("stores cause", () => {
		const cause = new Error("root cause");
		const error = new InvalidAddressLengthError(undefined, { cause });
		expect(error.cause).toBe(cause);
	});
});

describe("InvalidValueError", () => {
	it("creates error with message", () => {
		const error = new InvalidValueError("Value is invalid");
		expect(error.message).toContain("Value is invalid");
		expect(error.name).toBe("InvalidValueError");
	});

	it("sets error code", () => {
		const error = new InvalidValueError("Invalid");
		expect(error.code).toBe("INVALID_VALUE");
	});

	it("sets custom error code", () => {
		const error = new InvalidValueError("Invalid", {
			code: "CUSTOM_CODE",
		});
		expect(error.code).toBe("CUSTOM_CODE");
	});

	it("stores invalid value", () => {
		const error = new InvalidValueError("Invalid", {
			value: 123,
		});
		expect(error.value).toBe(123);
	});

	it("sets expected value", () => {
		const error = new InvalidValueError("Invalid");
		expect(error.expected).toBe("Valid value");
	});

	it("sets custom expected value", () => {
		const error = new InvalidValueError("Invalid", {
			expected: "positive number",
		});
		expect(error.expected).toBe("positive number");
	});

	it("sets docs path", () => {
		const error = new InvalidValueError("Invalid");
		expect(error.docsPath).toBe(
			"/primitives/address/fundamentals#error-handling",
		);
	});

	it("stores context", () => {
		const error = new InvalidValueError("Invalid", {
			context: { min: 0, max: 100 },
		});
		expect(error.context).toEqual({ min: 0, max: 100 });
	});

	it("stores cause", () => {
		const cause = new Error("root cause");
		const error = new InvalidValueError("Invalid", { cause });
		expect(error.cause).toBe(cause);
	});
});

describe("NotImplementedError", () => {
	it("creates error with default message", () => {
		const error = new NotImplementedError();
		expect(error.message).toBe("Not implemented");
		expect(error.name).toBe("NotImplementedError");
	});

	it("creates error with custom message", () => {
		const error = new NotImplementedError("Feature not implemented");
		expect(error.message).toBe("Feature not implemented");
	});

	it("sets error code", () => {
		const error = new NotImplementedError();
		expect(error.code).toBe("NOT_IMPLEMENTED");
	});

	it("sets custom error code", () => {
		const error = new NotImplementedError(undefined, {
			code: "CUSTOM_CODE",
		});
		expect(error.code).toBe("CUSTOM_CODE");
	});

	it("stores context", () => {
		const error = new NotImplementedError(undefined, {
			context: { feature: "wasm" },
		});
		expect(error.context).toEqual({ feature: "wasm" });
	});

	it("sets docs path", () => {
		const error = new NotImplementedError(undefined, {
			docsPath: "/custom/path",
		});
		expect(error.docsPath).toBe("/custom/path");
	});

	it("stores cause", () => {
		const cause = new Error("root cause");
		const error = new NotImplementedError(undefined, { cause });
		expect(error.cause).toBe(cause);
	});

	it("can be thrown and caught", () => {
		expect(() => {
			throw new NotImplementedError("Test feature");
		}).toThrow(NotImplementedError);
	});

	it("instanceof works correctly", () => {
		const error = new NotImplementedError();
		expect(error instanceof NotImplementedError).toBe(true);
		expect(error instanceof Error).toBe(true);
	});
});

describe("Error inheritance", () => {
	it("InvalidHexFormatError is instanceof Error", () => {
		const error = new InvalidHexFormatError();
		expect(error instanceof Error).toBe(true);
	});

	it("InvalidHexStringError is instanceof Error", () => {
		const error = new InvalidHexStringError();
		expect(error instanceof Error).toBe(true);
	});

	it("InvalidAddressLengthError is instanceof Error", () => {
		const error = new InvalidAddressLengthError();
		expect(error instanceof Error).toBe(true);
	});

	it("InvalidValueError is instanceof Error", () => {
		const error = new InvalidValueError("Invalid");
		expect(error instanceof Error).toBe(true);
	});

	it("NotImplementedError is instanceof Error", () => {
		const error = new NotImplementedError();
		expect(error instanceof Error).toBe(true);
	});
});

describe("Error serialization", () => {
	it("InvalidHexFormatError has stack trace", () => {
		const error = new InvalidHexFormatError();
		expect(error.stack).toBeDefined();
	});

	it("InvalidHexStringError has stack trace", () => {
		const error = new InvalidHexStringError();
		expect(error.stack).toBeDefined();
	});

	it("InvalidAddressLengthError has stack trace", () => {
		const error = new InvalidAddressLengthError();
		expect(error.stack).toBeDefined();
	});

	it("InvalidValueError has stack trace", () => {
		const error = new InvalidValueError("Invalid");
		expect(error.stack).toBeDefined();
	});

	it("NotImplementedError has stack trace", () => {
		const error = new NotImplementedError();
		expect(error.stack).toBeDefined();
	});
});

```
