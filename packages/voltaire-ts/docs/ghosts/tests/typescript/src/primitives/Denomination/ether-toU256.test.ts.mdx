---
title: '[TS/JS] src/primitives/Denomination/ether-toU256.test.ts'
source: 'src/primitives/Denomination/ether-toU256.test.ts'
---

> Auto-generated from test file: src/primitives/Denomination/ether-toU256.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { from } from "./ether-from.js";
import { toU256 } from "./ether-toU256.js";

describe("toU256", () => {
	it("converts Ether to Uint256 (Wei)", () => {
		const ether = from("1");
		const u256 = toU256(ether);
		expect(typeof u256).toBe("bigint");
		expect(u256).toBe(1_000_000_000_000_000_000n);
	});

	it("converts zero Ether", () => {
		const ether = from("0");
		const u256 = toU256(ether);
		expect(u256).toBe(0n);
	});

	it("converts large Ether value", () => {
		const ether = from("1000000");
		const u256 = toU256(ether);
		expect(u256).toBe(1_000_000_000_000_000_000_000_000n);
	});

	it("converts fractional Ether", () => {
		const ether = from("1.5");
		const u256 = toU256(ether);
		expect(u256).toBe(1_500_000_000_000_000_000n);
	});
});

```
