---
title: '[TS/JS] src/primitives/Slot/toBigInt.test.ts'
source: 'src/primitives/Slot/toBigInt.test.ts'
---

> Auto-generated from test file: src/primitives/Slot/toBigInt.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as Slot from "./index.js";

describe("Slot.toBigInt", () => {
	describe("conversion", () => {
		it("converts to bigint", () => {
			const slot = Slot.from(1000000);
			expect(Slot.toBigInt(slot)).toBe(1000000n);
		});

		it("converts zero", () => {
			const slot = Slot.from(0);
			expect(Slot.toBigInt(slot)).toBe(0n);
		});

		it("converts large value", () => {
			const large = 2n ** 100n;
			const slot = Slot.from(large);
			expect(Slot.toBigInt(slot)).toBe(large);
		});

		it("converts from number input", () => {
			const slot = Slot.from(42);
			expect(Slot.toBigInt(slot)).toBe(42n);
		});

		it("converts from string input", () => {
			const slot = Slot.from("12345");
			expect(Slot.toBigInt(slot)).toBe(12345n);
		});
	});

	describe("round-trip", () => {
		it("preserves value through from and toBigInt", () => {
			const original = 1234567n;
			const slot = Slot.from(original);
			const result = Slot.toBigInt(slot);
			expect(result).toBe(original);
		});

		it("preserves zero", () => {
			const original = 0n;
			const slot = Slot.from(original);
			const result = Slot.toBigInt(slot);
			expect(result).toBe(original);
		});

		it("preserves max safe integer", () => {
			const original = BigInt(Number.MAX_SAFE_INTEGER);
			const slot = Slot.from(original);
			const result = Slot.toBigInt(slot);
			expect(result).toBe(original);
		});
	});

	describe("type consistency", () => {
		it("returns bigint type", () => {
			const slot = Slot.from(100);
			const result = Slot.toBigInt(slot);
			expect(typeof result).toBe("bigint");
		});
	});
});

```
