---
title: '[TS/JS] src/primitives/Hardfork/hasEIP3855.test.ts'
source: 'src/primitives/Hardfork/hasEIP3855.test.ts'
---

> Auto-generated from test file: src/primitives/Hardfork/hasEIP3855.test.ts

```typescript
/**
 * Tests for Hardfork.hasEIP3855 (PUSH0)
 */

import { describe, expect, it } from "vitest";
import { hasEIP3855 } from "./hasEIP3855.js";
import * as Hardfork from "./index.js";

describe("Hardfork.hasEIP3855", () => {
	it("returns false for Merge", () => {
		expect(hasEIP3855(Hardfork.MERGE)).toBe(false);
	});

	it("returns false for London", () => {
		expect(hasEIP3855(Hardfork.LONDON)).toBe(false);
	});

	it("returns false for Berlin", () => {
		expect(hasEIP3855(Hardfork.BERLIN)).toBe(false);
	});

	it("returns true for Shanghai", () => {
		expect(hasEIP3855(Hardfork.SHANGHAI)).toBe(true);
	});

	it("returns true for Cancun", () => {
		expect(hasEIP3855(Hardfork.CANCUN)).toBe(true);
	});

	it("returns true for Prague", () => {
		expect(hasEIP3855(Hardfork.PRAGUE)).toBe(true);
	});

	it("returns false for all pre-Shanghai hardforks", () => {
		const preShanghai = [
			Hardfork.FRONTIER,
			Hardfork.HOMESTEAD,
			Hardfork.DAO,
			Hardfork.TANGERINE_WHISTLE,
			Hardfork.SPURIOUS_DRAGON,
			Hardfork.BYZANTIUM,
			Hardfork.CONSTANTINOPLE,
			Hardfork.PETERSBURG,
			Hardfork.ISTANBUL,
			Hardfork.MUIR_GLACIER,
			Hardfork.BERLIN,
			Hardfork.LONDON,
			Hardfork.ARROW_GLACIER,
			Hardfork.GRAY_GLACIER,
			Hardfork.MERGE,
		];

		for (const fork of preShanghai) {
			expect(hasEIP3855(fork)).toBe(false);
		}
	});
});

```
