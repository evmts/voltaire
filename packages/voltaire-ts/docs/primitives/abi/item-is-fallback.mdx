---
title: Item.isFallback
description: Type guard for fallback ABI items
---

## Overview

`Item.isFallback` checks whether an ABI item represents a fallback function.

## Example

```typescript
import { Abi } from '@tevm/voltaire/Abi';

const item = { type: 'fallback', stateMutability: 'payable' } as const;

if (Abi.Item.isFallback(item)) {
  // Handle fallback definition
}
```

## See Also

- [Item.isReceive](/primitives/abi/item-is-receive)
- [Item.isFunction](/primitives/abi/item-is-function)
