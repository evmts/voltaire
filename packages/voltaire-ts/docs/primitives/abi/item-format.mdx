---
title: Item.format
description: Format a single ABI item to a human-readable string
---

## Overview

`Item.format` formats a single ABI item (function/event/error/etc.) as a readable signature string.

## Quick Start

```typescript
import { Abi } from '@tevm/voltaire/Abi';

const fn = {
  type: 'function',
  name: 'balanceOf',
  inputs: [{ type: 'address', name: 'owner' }],
  outputs: [{ type: 'uint256' }],
  stateMutability: 'view'
} as const;

const formatted = Abi.Item.format(fn);
// "function balanceOf(address owner) returns (uint256) view"
```

## See Also

- [Abi.format](/primitives/abi/format) - Top-level formatter
- [Item.formatWithArgs](/primitives/abi/item-format-with-args) - Format with arguments
