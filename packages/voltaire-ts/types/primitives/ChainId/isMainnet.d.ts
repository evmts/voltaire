/**
 * Check if chain ID is Ethereum mainnet
 *
 * @this {number}
 * @returns {boolean} True if mainnet
 *
 * @example
 * ```typescript
 * const isMain = ChainId._isMainnet.call(chainId);
 * ```
 */
export function isMainnet(this: number): boolean;
//# sourceMappingURL=isMainnet.d.ts.map