/**
 * Check if two hardforks are equal
 *
 * @param {import('./HardforkType.js').HardforkType} a - First hardfork
 * @param {import('./HardforkType.js').HardforkType} b - Second hardfork
 * @returns {boolean} true if a == b
 *
 * @example
 * ```typescript
 * import { CANCUN, equals } from './hardfork.js';
 *
 * if (equals(fork, CANCUN)) {
 *   // Exactly Cancun
 * }
 * ```
 */
export function equals(a: import("./HardforkType.js").HardforkType, b: import("./HardforkType.js").HardforkType): boolean;
//# sourceMappingURL=equals.d.ts.map