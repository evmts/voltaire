import { Bytes6 } from "@tevm/voltaire/Bytes";

export type { Bytes6Type } from "@tevm/voltaire/Bytes";

export const {
	BytesType6,
	from,
	fromHex,
	toHex,
	toBytes,
	equals,
	compare,
	size,
	clone,
} = Bytes6;

export { Bytes6 };
