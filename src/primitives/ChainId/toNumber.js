/**
 * Convert ChainId to number
 *
 * @param this - Chain ID
 * @returns Number
 *
 * @example
 * ```typescript
 * const n = ChainId._toNumber.call(chainId);
 * ```
 */
export function toNumber() {
	return this;
}
