const std = @import("std");

// Minimal test to verify RED phase
test "Instruction.execute should fail - RED phase" {
    // This test should fail to prove our tests are running
    try std.testing.expect(false);
}