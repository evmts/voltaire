---
title: Address
description: Effect-based schemas and functions for Ethereum addresses with EIP-55 checksum support
---

```typescript
import * as Address from 'voltaire-effect/primitives/Address'
import { Effect } from 'effect'

const addr = await Effect.runPromise(
  Address.from('0x742d35Cc6634C0532925a3b844Bc9e7595f251e3')
)
```

## Schemas

**AddressSchema** — Validates hex string, returns branded `Uint8Array`.

```typescript
import { AddressSchema } from 'voltaire-effect/primitives/Address'
import * as Schema from 'effect/Schema'

const addr = Schema.decodeSync(AddressSchema)(
  '0x742d35Cc6634C0532925a3b844Bc9e7595f251e3'
)
```

**AddressFromBytesSchema** — Validates 20-byte array.

**ChecksummedAddressSchema** — Encodes to EIP-55 format.

## Functions

**from(input)** — Creates address from string, bytes, number, or bigint.

```typescript
Address.from('0x742d35Cc6634C0532925a3b844Bc9e7595f251e3')
Address.from(new Uint8Array(20).fill(0xab))
Address.from(0x742d35Cc6634C0532925a3b844Bc9e7595f251e3n)
// Effect<AddressType, InvalidAddressError>
```

**fromBytes(bytes)** — Creates from 20-byte Uint8Array. Returns `InvalidAddressLengthError` if wrong size.

**toBytes(addr)** — Pure sync. Returns `Uint8Array(20)`.

**toChecksummed(addr)** — Requires `KeccakService`.

```typescript
import { KeccakLive } from 'voltaire-effect/crypto/Keccak256'

const checksummed = await Effect.runPromise(
  Address.toChecksummed(addr).pipe(Effect.provide(KeccakLive))
)
// "0x5aAeb6053F3E94C9b9A09f33669435E7Ef1BeAed"
```

## Errors

- `InvalidAddressError` — Invalid format, hex, or value
- `InvalidAddressLengthError` — Byte array not 20 bytes

## See Also

- [PublicKey](/primitives/publickey) - Derive address from public key
- [PrivateKey](/primitives/privatekey) - Generate addresses from private keys
- [ENS](/primitives/ens) - Resolve ENS names to addresses
