import { Bytes1 } from "@tevm/voltaire/Bytes";

export type { Bytes1Type } from "@tevm/voltaire/Bytes";

export const {
	from,
	fromHex,
	fromNumber,
	toHex,
	toNumber,
	toBytes,
	equals,
	compare,
	size,
	clone,
} = Bytes1;

export { Bytes1 };
