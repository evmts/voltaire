import { Event } from "@tevm/voltaire/Abi";

export const {
	getSignature,
	getSelector,
	encodeTopics,
	decodeLog,
	Signature,
	Topics,
	DecodeLog,
	GetSelector,
	EncodeTopics,
} = Event;

export { Event };
