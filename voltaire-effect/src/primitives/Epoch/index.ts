/**
 * Epoch module for working with beacon chain epochs.
 * Provides Effect-based operations for creating and converting epochs.
 *
 * ## Type Declarations
 *
 * ```typescript
 * import * as Epoch from 'voltaire-effect/primitives/Epoch'
 *
 * function getEpochRewards(epoch: Epoch.EpochType) {
 *   // ...
 * }
 * ```
 *
 * @module
 * @since 0.0.1
 */
export { EpochSchema, type EpochType } from "./BigInt.js";
