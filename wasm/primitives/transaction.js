/**
 * Transaction type definitions for WASM signers
 * These are JavaScript runtime type definitions (no TypeScript here)
 */

// Note: Since this is JavaScript and types are runtime,
// we just export empty objects that can be used for JSDoc
// The actual type checking happens at the TypeScript comparison level

module.exports = {
	// Type exports are just for documentation
	// The actual transaction objects are plain JavaScript objects
};
