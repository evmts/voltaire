import * as Hash from "../../../primitives/Hash/index.js";

// Example: Generating random hashes

// Generate random hashes
const random1 = Hash.random();
const random2 = Hash.random();
const random3 = Hash.random();
