---
title: Tevm
description: High-performance, type-safe Ethereum primitives and cryptography
mode: "custom"
---

<style>{`
  @media (prefers-color-scheme: dark) {
    #navbar, [id="navbar"], nav {
      background-color: #280D2E !important;
    }
  }
  .dark #navbar,
  .dark [id="navbar"],
  .dark nav {
    background-color: #280D2E !important;
  }
`}</style>

<div style={{ textAlign: 'center', padding: '3rem 2rem 6rem', maxWidth: '1280px', margin: '0 auto' }}>
    <h1 style={{ fontSize: 'clamp(3rem, 8vw, 6rem)', fontWeight: '800', lineHeight: '1.1', marginBottom: '1.5rem' }}>
      <span style={{ display: 'block', background: 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)', WebkitBackgroundClip: 'text', WebkitTextFillColor: 'transparent', backgroundClip: 'text' }}>Tevm</span>
    </h1>

    <p style={{ fontSize: 'clamp(1.2rem, 3vw, 1.5rem)', color: '#999', maxWidth: '800px', margin: '0 auto 3rem', lineHeight: '1.6' }}><strong>Modern, high-performance Ethereum library for <span style={{ position: 'relative', display: 'inline-block', overflow: 'hidden', verticalAlign: 'middle', top: '-0.1em', width: '10ch', textAlign: 'left' }}><span id="language-text" style={{ display: 'inline-block', color: '#3178C6', transition: 'transform 0.2s ease-out' }}>TypeScript</span></span></strong></p>

    <div style={{ display: 'flex', gap: '1rem', justifyContent: 'center', flexWrap: 'wrap', marginBottom: '3rem' }}>
      <a href="/introduction"
         style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', padding: '0.75rem 2rem', borderRadius: '8px', textDecoration: 'none', fontWeight: '600', background: 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)', color: 'white', transition: 'all 0.15s ease', transform: 'scale(1)', boxShadow: 'none' }}
         onMouseEnter={(e) => { e.currentTarget.style.transform = 'scale(1.02)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(230, 162, 60, 0.3)'; }}
         onMouseLeave={(e) => { e.currentTarget.style.transform = 'scale(1)'; e.currentTarget.style.boxShadow = 'none'; }}
         onMouseDown={(e) => { e.currentTarget.style.transform = 'scale(0.99)'; }}
         onMouseUp={(e) => { e.currentTarget.style.transform = 'scale(1.02)'; }}>
        Why Tevm?
      </a>
      <a href="/getting-started"
         style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', padding: '0.75rem 2rem', borderRadius: '8px', textDecoration: 'none', fontWeight: '600', background: 'transparent', color: 'currentColor', border: '2px solid rgba(128, 128, 128, 0.3)', transition: 'all 0.15s ease', transform: 'scale(1)', boxShadow: 'none' }}
         onMouseEnter={(e) => { e.currentTarget.style.background = 'rgba(128, 128, 128, 0.1)'; e.currentTarget.style.borderColor = 'rgba(128, 128, 128, 0.5)'; e.currentTarget.style.transform = 'scale(1.02)'; }}
         onMouseLeave={(e) => { e.currentTarget.style.background = 'transparent'; e.currentTarget.style.borderColor = 'rgba(128, 128, 128, 0.3)'; e.currentTarget.style.transform = 'scale(1)'; }}
         onMouseDown={(e) => { e.currentTarget.style.transform = 'scale(0.99)'; }}
         onMouseUp={(e) => { e.currentTarget.style.transform = 'scale(1.02)'; }}>
        Get Started
      </a>
      <a href="https://github.com/evmts/tevm" target="_blank" rel="noopener"
         style={{ display: 'inline-flex', alignItems: 'center', justifyContent: 'center', padding: '0.75rem 2rem', borderRadius: '8px', textDecoration: 'none', fontWeight: '600', background: 'transparent', color: 'currentColor', border: '2px solid rgba(128, 128, 128, 0.3)', transition: 'all 0.15s ease', transform: 'scale(1)', boxShadow: 'none' }}
         onMouseEnter={(e) => { e.currentTarget.style.background = 'rgba(128, 128, 128, 0.1)'; e.currentTarget.style.borderColor = 'rgba(128, 128, 128, 0.5)'; e.currentTarget.style.transform = 'scale(1.02)'; }}
         onMouseLeave={(e) => { e.currentTarget.style.background = 'transparent'; e.currentTarget.style.borderColor = 'rgba(128, 128, 128, 0.3)'; e.currentTarget.style.transform = 'scale(1)'; }}
         onMouseDown={(e) => { e.currentTarget.style.transform = 'scale(0.99)'; }}
         onMouseUp={(e) => { e.currentTarget.style.transform = 'scale(1.02)'; }}>
        View on GitHub
      </a>
    </div>

    <div style={{ maxWidth: '1000px', margin: '0 auto 5rem' }}>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '1.5rem' }}>
        <a href="/getting-started/llm-optimized" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>📐</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>Spec-Aligned API</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>Primitives map directly to Ethereum specifications</p>
          </div>
        </a>
        <a href="/getting-started/branded-types" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>🛡️</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>Zero-Cost Type Safety</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>Branded types provide compile-time safety with zero runtime overhead</p>
          </div>
        </a>
        <a href="/getting-started/wasm" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>⚡</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>High Performance Zig</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>High-performance implementations for cryptography, encoding, and EVM execution written in Zig</p>
          </div>
        </a>
        <a href="/getting-started/llm-optimized" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>🤖</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>AI-Native</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>MCP server integration and llms.txt for seamless AI tool usage</p>
          </div>
        </a>
        <a href="/getting-started/powerful-features" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>📦</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>Powerful Features</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>Unique features such as EVM execution and bytecode analysis</p>
          </div>
        </a>
        <a href="/getting-started/multiplatform" style={{ display: 'flex', alignItems: 'center', gap: '1rem', padding: '1.5rem', border: '1px solid rgba(255, 255, 255, 0.1)', borderRadius: '12px', textDecoration: 'none', transition: 'all 0.15s ease', cursor: 'pointer' }} onMouseEnter={(e) => { e.currentTarget.style.transform = 'translateY(-2px)'; e.currentTarget.style.boxShadow = '0 4px 12px rgba(212, 147, 30, 0.2)'; e.currentTarget.style.borderColor = 'rgba(212, 147, 30, 0.3)'; }} onMouseLeave={(e) => { e.currentTarget.style.transform = 'translateY(0)'; e.currentTarget.style.boxShadow = 'none'; e.currentTarget.style.borderColor = 'rgba(255, 255, 255, 0.1)'; }}>
          <div style={{ fontSize: '2rem', color: '#D4931E', flexShrink: 0 }}>🌐</div>
          <div>
            <h3 style={{ margin: '0 0 0.5rem 0', fontSize: '1.1rem', fontWeight: '700', color: '#D4931E' }}>Multi-Platform</h3>
            <p style={{ margin: 0, fontSize: '0.9rem', color: '#999' }}>Works in browser, mobile, Node.js, and native environments. Supports Zig and C-FFI</p>
          </div>
        </a>
      </div>
    </div>

    <div style={{ maxWidth: '900px', margin: '0 auto 4rem', position: 'relative' }}>
      <div role="tablist" style={{ display: 'flex', gap: '0.5rem', justifyContent: 'center', marginBottom: '1rem' }}>
        <button role="tab" aria-selected="true" aria-controls="claude-code" aria-label="Claude MCP installation" id="claude-btn" style={{ padding: '0.5rem 1.5rem', borderRadius: '6px', border: '2px solid #E6A23C', background: 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)', color: 'white', fontWeight: '600', cursor: 'pointer', transition: 'all 0.15s ease' }} onClick={(e) => {
          document.getElementById('claude-btn').style.background = 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)';
          document.getElementById('claude-btn').style.color = 'white';
          document.getElementById('claude-btn').style.borderColor = '#E6A23C';
          document.getElementById('cursor-btn').style.background = 'transparent';
          document.getElementById('cursor-btn').style.color = 'currentColor';
          document.getElementById('cursor-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('codex-btn').style.background = 'transparent';
          document.getElementById('codex-btn').style.color = 'currentColor';
          document.getElementById('codex-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('claude-code').style.display = 'block';
          document.getElementById('cursor-code').style.display = 'none';
          document.getElementById('codex-code').style.display = 'none';
        }}>
          Claude
        </button>
        <button role="tab" aria-selected="false" aria-controls="cursor-code" aria-label="Cursor MCP installation" id="cursor-btn" style={{ padding: '0.5rem 1.5rem', borderRadius: '6px', border: '2px solid rgba(128, 128, 128, 0.3)', background: 'transparent', color: 'currentColor', fontWeight: '600', cursor: 'pointer', transition: 'all 0.15s ease' }} onClick={(e) => {
          document.getElementById('cursor-btn').style.background = 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)';
          document.getElementById('cursor-btn').style.color = 'white';
          document.getElementById('cursor-btn').style.borderColor = '#E6A23C';
          document.getElementById('claude-btn').style.background = 'transparent';
          document.getElementById('claude-btn').style.color = 'currentColor';
          document.getElementById('claude-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('codex-btn').style.background = 'transparent';
          document.getElementById('codex-btn').style.color = 'currentColor';
          document.getElementById('codex-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('claude-code').style.display = 'none';
          document.getElementById('cursor-code').style.display = 'block';
          document.getElementById('codex-code').style.display = 'none';
        }}>
          Cursor
        </button>
        <button role="tab" aria-selected="false" aria-controls="codex-code" aria-label="Codex MCP installation" id="codex-btn" style={{ padding: '0.5rem 1.5rem', borderRadius: '6px', border: '2px solid rgba(128, 128, 128, 0.3)', background: 'transparent', color: 'currentColor', fontWeight: '600', cursor: 'pointer', transition: 'all 0.15s ease' }} onClick={(e) => {
          document.getElementById('codex-btn').style.background = 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)';
          document.getElementById('codex-btn').style.color = 'white';
          document.getElementById('codex-btn').style.borderColor = '#E6A23C';
          document.getElementById('claude-btn').style.background = 'transparent';
          document.getElementById('claude-btn').style.color = 'currentColor';
          document.getElementById('claude-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('cursor-btn').style.background = 'transparent';
          document.getElementById('cursor-btn').style.color = 'currentColor';
          document.getElementById('cursor-btn').style.borderColor = 'rgba(128, 128, 128, 0.3)';
          document.getElementById('claude-code').style.display = 'none';
          document.getElementById('cursor-code').style.display = 'none';
          document.getElementById('codex-code').style.display = 'block';
        }}>
          Codex
        </button>
      </div>
      <div id="copy-feedback" style={{ position: 'absolute', top: '-2.5rem', right: '1rem', background: 'linear-gradient(135deg, #E6A23C 0%, #A0522D 100%)', color: 'white', padding: '0.5rem 1rem', borderRadius: '6px', fontSize: '0.875rem', fontWeight: '600', opacity: '0', transition: 'opacity 0.3s', pointerEvents: 'none', zIndex: 10 }}>
        Copied!
      </div>
      <div id="claude-code" style={{ cursor: 'pointer' }}
           onMouseDown={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.transition = 'box-shadow 0.1s ease';
          codeBlock.style.boxShadow = '0 0 0 3px #E6A23C, 0 0 0 6px #A0522D';
        }
      }}
           onMouseUp={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onMouseLeave={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onClick={(e) => {
        navigator.clipboard.writeText('claude mcp add --transport http tevm https://tevm.sh/mcp');
        const feedback = document.getElementById('copy-feedback');
        feedback.style.opacity = '1';
        setTimeout(() => {
          feedback.style.opacity = '0';
        }, 500);
      }}>
```bash
claude mcp add --transport http tevm https://tevm.sh/mcp
```
      </div>
      <div id="cursor-code" style={{ cursor: 'pointer', display: 'none' }}
           onMouseDown={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.transition = 'box-shadow 0.1s ease';
          codeBlock.style.boxShadow = '0 0 0 3px #E6A23C, 0 0 0 6px #A0522D';
        }
      }}
           onMouseUp={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onMouseLeave={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onClick={(e) => {
        navigator.clipboard.writeText('Add to .cursor/mcp.json:\n{\n  "mcpServers": {\n    "tevm": {\n      "command": "npx",\n      "args": ["mcp-remote", "https://tevm.sh/mcp"]\n    }\n  }\n}');
        const feedback = document.getElementById('copy-feedback');
        feedback.style.opacity = '1';
        setTimeout(() => {
          feedback.style.opacity = '0';
        }, 500);
      }}>
```json
// Add to .cursor/mcp.json
{
  "mcpServers": {
    "tevm": {
      "command": "npx",
      "args": ["mcp-remote", "https://tevm.sh/mcp"]
    }
  }
}
```
      </div>
      <div id="codex-code" style={{ cursor: 'pointer', display: 'none' }}
           onMouseDown={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.transition = 'box-shadow 0.1s ease';
          codeBlock.style.boxShadow = '0 0 0 3px #E6A23C, 0 0 0 6px #A0522D';
        }
      }}
           onMouseUp={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onMouseLeave={(e) => {
        const codeBlock = e.currentTarget.querySelector('[data-component-part="code-block-root"]');
        if (codeBlock) {
          codeBlock.style.boxShadow = '';
        }
      }}
           onClick={(e) => {
        navigator.clipboard.writeText('Add to ~/.codex/config.toml:\n[mcp_servers.tevm]\nurl = "https://tevm.sh/mcp"');
        const feedback = document.getElementById('copy-feedback');
        feedback.style.opacity = '1';
        setTimeout(() => {
          feedback.style.opacity = '0';
        }, 500);
      }}>
```toml
# Add to ~/.codex/config.toml
[mcp_servers.tevm]
url = "https://tevm.sh/mcp"
```
      </div>
    </div>

    <div id="editor-mount-point" style={{ maxWidth: '1000px', margin: '5rem auto 0' }}></div>

    <script dangerouslySetInnerHTML={{__html: `
      // Wait for full page load and React hydration
      window.addEventListener('load', function() {
        setTimeout(initMonaco, 500);
        setTimeout(initTextAnimation, 500);
      });

      function initTextAnimation() {
        // Language animation (TypeScript <-> WASM <-> Zig)
        const languages = [
          { name: 'TypeScript', color: '#3178C6' },
          { name: 'WASM', color: '#654FF0' },
          { name: 'Zig', color: '#F7A41D' }
        ];
        let langIndex = 0;
        const languageText = document.getElementById('language-text');

        if (!languageText) {
          console.error('language-text element not found');
          return;
        }

        function animateLanguage() {
          // Slide up (exit)
          languageText.style.transform = 'translateY(-100%)';

          setTimeout(() => {
            langIndex = (langIndex + 1) % languages.length;
            const lang = languages[langIndex];

            languageText.textContent = lang.name;
            languageText.style.color = lang.color;
            languageText.style.transform = 'translateY(100%)';

            setTimeout(() => {
              languageText.style.transform = 'translateY(0)';
            }, 50);
          }, 200);
        }

        // Start animation cycle (every 5 seconds)
        setInterval(animateLanguage, 5000);
      }

      async function initMonaco() {
        const mountPoint = document.getElementById('editor-mount-point');
        if (!mountPoint) return;

        mountPoint.innerHTML = \`
          <h2 style="font-size: clamp(2rem, 5vw, 2.5rem); font-weight: 700; text-align: center; margin-bottom: 1rem; background: linear-gradient(135deg, #E6A23C 0%, #A0522D 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">Try It Now</h2>
          <p style="font-size: 1.1rem; color: #999; text-align: center; margin-bottom: 2rem;">
            Edit and experiment with live Tevm code examples.
          </p>
          <div id="editor-container" style="width: 100%; height: 600px; border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 12px;"></div>
        \`;

        try {
          const { init } = await import('https://esm.sh/modern-monaco@0.2.2');
          const monaco = await init();

          const editor = monaco.editor.create(document.getElementById('editor-container'), {
            padding: { top: 8, bottom: 8 },
          });

          const code = \`// Generate Ethereum address from private key
import { Address, Hex } from "tevm";

const privateKey = Hex.toBytes(
  "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80"
);

const address = Address.fromPrivateKey(privateKey);

console.log("Address:", address.toHex());\`;

          const model = monaco.editor.createModel(code, 'typescript');
          editor.setModel(model);
        } catch (e) {
          document.getElementById('editor-container').innerHTML = '<div style="padding: 2rem; text-align: center; color: #999;">Failed to load editor</div>';
        }
      }
    `}}>
    </script>
  </div>

