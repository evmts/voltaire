---
title: Uint.bitwiseAnd
description: Bitwise AND operation on Uint256 values
---

<Tabs>
<Tab title="Class API (recommended)">

## `Uint.bitwiseAnd(a: BrandedUint, b: BrandedUint): BrandedUint`

Bitwise AND operation.

**Parameters:**
- `a: BrandedUint` - First value
- `b: BrandedUint` - Second value

**Returns:** `BrandedUint` - Bitwise AND result

**Example:**

```typescript
import { Uint } from '@tevm/voltaire'

const a = Uint.from(0b1100n)
const b = Uint.from(0b1010n)
Uint.bitwiseAnd(a, b)  // 0b1000 (8)

// Masking
const value = Uint.from(0xffn)
const mask = Uint.from(0x0fn)
Uint.bitwiseAnd(value, mask)  // 0x0f
```

**Defined in:** [primitives/Uint/BrandedUint/bitwiseAnd.ts](https://github.com/evmts/voltaire/blob/main/src/primitives/Uint/BrandedUint/bitwiseAnd.ts)

</Tab>
<Tab title="Namespace API (Tree-shakeable)">

## `bitwiseAnd(a: BrandedUint, b: BrandedUint): BrandedUint`

**Example:**

```typescript
import * as Uint from '@tevm/voltaire/Uint'

const result = Uint.bitwiseAnd(Uint.from(12n), Uint.from(10n))
```

**Defined in:** [primitives/Uint/BrandedUint/bitwiseAnd.ts](https://github.com/evmts/voltaire/blob/main/src/primitives/Uint/BrandedUint/bitwiseAnd.ts)

</Tab>
<Tab title="Zig">

## `Uint.bitwiseAnd(a: Uint, b: Uint): Uint`

**Example:**

```zig
const Uint = @import("primitives").Uint;

const a = Uint.init(12);
const b = Uint.init(10);
const result = Uint.bitwiseAnd(a, b);
```

**Defined in:** [primitives/Uint/uint.zig](https://github.com/evmts/voltaire/blob/main/src/primitives/Uint/uint.zig)

</Tab>
</Tabs>

## See Also

- [bitwiseOr](/primitives/uint/bitwise-or) - Bitwise OR
- [bitwiseXor](/primitives/uint/bitwise-xor) - Bitwise XOR
- [Bitwise operations](/primitives/uint/bitwise) - Full bitwise reference
