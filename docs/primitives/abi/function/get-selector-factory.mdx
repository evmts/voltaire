---
title: Abi.Function.GetSelector
description: Build a function selector helper with a custom keccak256 implementation
---

## Overview

`Function.GetSelector` creates a selector function using a custom `keccak256String`. Use this for alternative hashing backends (native/WASM/custom).

## Quick Start

```typescript
import { Abi } from '@tevm/voltaire/Abi';

const getSelector = Abi.Function.GetSelector({
  keccak256String: (value) => myKeccak256String(value)
});

const transfer = {
  type: 'function',
  name: 'transfer',
  inputs: [
    { type: 'address', name: 'to' },
    { type: 'uint256', name: 'amount' }
  ],
  outputs: [{ type: 'bool' }]
} as const;

const selector = getSelector(transfer);
```

## See Also

- [getSelector](/primitives/abi/function/get-selector) - Default selector implementation
- [getSignature](/primitives/abi/function/get-signature) - Function signature string
