---
title: Blob.isValidVersion
description: Validate versioned hash format and version byte
---

<Tabs>
<Tab title="Class API">

## Signature

```typescript
Blob.isValidVersion(hash: Uint8Array): boolean
```

Validate versioned hash format. Checks length (32 bytes) and version byte (0x01 for KZG).

### Parameters

- **hash** `Uint8Array` - Hash to validate

### Returns

`boolean` - `true` if valid versioned hash format, `false` otherwise

### Example

```typescript
import { Blob } from '@tevm/voltaire';

const blob = Blob.fromData(data);
const versionedHash = blob.toVersionedHash();

// Validate format
console.log(Blob.isValidVersion(versionedHash)); // true

// Invalid: wrong length
const tooShort = new Uint8Array(16);
console.log(Blob.isValidVersion(tooShort)); // false

// Invalid: wrong version byte
const wrongVersion = new Uint8Array(32);
wrongVersion[0] = 0x02; // Unsupported version
console.log(Blob.isValidVersion(wrongVersion)); // false

// Valid: correct format
const valid = new Uint8Array(32);
valid[0] = 0x01; // KZG version
console.log(Blob.isValidVersion(valid)); // true
```

### Example: Transaction Validation

```typescript
import { Blob } from '@tevm/voltaire';

function validateBlobTransaction(tx: BlobTransaction): boolean {
  // Validate versioned hashes
  for (const hash of tx.blobVersionedHashes) {
    if (!Blob.isValidVersion(hash)) {
      console.error('Invalid versioned hash format');
      return false;
    }
  }

  // Check blob count matches hashes
  if (tx.blobs.length !== tx.blobVersionedHashes.length) {
    console.error('Blob count mismatch');
    return false;
  }

  return true;
}
```

### Defined in

[src/primitives/Blob/BrandedBlob/isValidVersion.js](https://github.com/tevm/voltaire/blob/main/src/primitives/Blob/BrandedBlob/isValidVersion.js)

</Tab>
<Tab title="Namespace API">

## Signature

```typescript
isValidVersion(hash: Uint8Array): boolean
```

Validate versioned hash format. Checks length (32 bytes) and version byte (0x01 for KZG).

### Parameters

- **hash** `Uint8Array` - Hash to validate

### Returns

`boolean` - `true` if valid versioned hash format, `false` otherwise

### Example

```typescript
import * as Blob from '@tevm/voltaire/Blob';

const versionedHash = Blob.toVersionedHash(blob);
console.log(Blob.isValidVersion(versionedHash)); // true
```

### Defined in

[src/primitives/Blob/BrandedBlob/isValidVersion.js](https://github.com/tevm/voltaire/blob/main/src/primitives/Blob/BrandedBlob/isValidVersion.js)

</Tab>
<Tab title="Zig">

## Signature

```zig
pub fn isValidVersion(hash: []const u8) bool
```

Validate versioned hash format.

### Parameters

- **hash** `[]const u8` - Hash to validate

### Returns

`bool` - `true` if valid format

### Example

```zig
const Blob = @import("primitives").Blob;

const versioned_hash = try blob.toVersionedHash(allocator);
defer versioned_hash.deinit(allocator);

const valid = Blob.isValidVersion(versioned_hash);
std.debug.print("Valid: {}\n", .{valid});
```

</Tab>
</Tabs>

## Validation Rules

A valid versioned hash must:
1. Be exactly 32 bytes
2. Have version byte 0x01 (KZG commitment version)

```typescript
import { Blob } from '@tevm/voltaire';

function isValidVersionedHash(hash: Uint8Array): boolean {
  // Check length
  if (hash.length !== 32) {
    return false;
  }

  // Check version byte
  if (hash[0] !== 0x01) {
    return false;
  }

  return true;
}

// Equivalent to Blob.isValidVersion()
```

## Version Byte

Current supported version:

```typescript
import { Blob } from '@tevm/voltaire';

console.log(Blob.COMMITMENT_VERSION_KZG); // 0x01

const hash = blob.toVersionedHash();
console.log(hash[0]); // 0x01

// Future versions (not yet supported)
// 0x02 = Future commitment scheme
// 0x03 = Another scheme
```

## VersionedHash Utilities

```typescript
import { Blob } from '@tevm/voltaire';

const versionedHash = blob.toVersionedHash();

// Validate format
console.log(Blob.isValidVersion(versionedHash)); // true

// Check specific validation using VersionedHash namespace
console.log(Blob.VersionedHash.isValid(versionedHash)); // true

// Get version byte
const version = Blob.VersionedHash.getVersion(versionedHash);
console.log(version); // 1

// Alternative: version() method
console.log(Blob.VersionedHash.version(versionedHash)); // 1
```

## Transaction Validation

```typescript
import { Blob } from '@tevm/voltaire';

function validateBlobVersionedHashes(
  hashes: Uint8Array[],
  maxBlobs: number = Blob.MAX_PER_TRANSACTION
): boolean {
  // Check count
  if (hashes.length === 0 || hashes.length > maxBlobs) {
    return false;
  }

  // Validate each hash
  for (const hash of hashes) {
    if (!Blob.isValidVersion(hash)) {
      return false;
    }
  }

  return true;
}

// Usage
const tx = { blobVersionedHashes: [hash1, hash2, hash3] };
if (!validateBlobVersionedHashes(tx.blobVersionedHashes)) {
  throw new Error('Invalid versioned hashes');
}
```

## Error Cases

```typescript
import { Blob } from '@tevm/voltaire';

// Wrong length
const tooShort = new Uint8Array(16);
console.log(Blob.isValidVersion(tooShort)); // false

const tooLong = new Uint8Array(64);
console.log(Blob.isValidVersion(tooLong)); // false

// Wrong version
const wrongVersion = new Uint8Array(32);
wrongVersion[0] = 0x02; // Unsupported
console.log(Blob.isValidVersion(wrongVersion)); // false

wrongVersion[0] = 0x00; // Invalid
console.log(Blob.isValidVersion(wrongVersion)); // false

// Correct
const correct = new Uint8Array(32);
correct[0] = 0x01; // KZG version
console.log(Blob.isValidVersion(correct)); // true
```

## See Also

- [toVersionedHash](./toVersionedHash) - Compute versioned hash
- [versioned-hash](./versioned-hash) - VersionedHash namespace utilities
- [EIP-4844](/primitives/blob/eip4844) - Versioned hash specification
