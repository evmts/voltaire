{
  "name": "guillotine-docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vocs dev",
    "build": "npx playwright install chromium && npx playwright install-deps && vocs build",
    "preview": "vocs preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "playwright": "^1.55.0",
    "vercel": "^47.0.7",
    "vocs": "^1.0.13"
  }
}
