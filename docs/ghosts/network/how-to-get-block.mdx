---
title: How to Get Block by Number or Hash
description: Query block data by block number or block hash
---

# Get Block by Number or Hash

## By Block Number

```typescript
import { createJsonRpcProvider } from '@tevm/voltaire/jsonrpc';
import * as Rpc from '@tevm/voltaire/jsonrpc';

const provider = createJsonRpcProvider('https://eth.llamarpc.com');

// Get latest block with full transactions
const block = await provider.request(
  Rpc.Eth.GetBlockByNumberRequest('latest', true)
);

console.log('Block number:', block.number);
console.log('Timestamp:', block.timestamp);
console.log('Transactions:', block.transactions.length);

// Get specific block (tx hashes only)
const historicalBlock = await provider.request(
  Rpc.Eth.GetBlockByNumberRequest('0xF4240', false) // Block 1,000,000
);
```

## By Block Hash

```typescript
import { createJsonRpcProvider } from '@tevm/voltaire/jsonrpc';
import * as Rpc from '@tevm/voltaire/jsonrpc';

const provider = createJsonRpcProvider('https://eth.llamarpc.com');

const blockHash = '0xd4e56740f876aef8c010b86a40d5f56745a118d0906a34e69aec8c0db1cb8fa3';

const block = await provider.request(
  Rpc.Eth.GetBlockByHashRequest(blockHash, false)
);

console.log('Block:', block.number);
```

## Block Tags

- `'latest'` - Most recent mined block
- `'earliest'` - Genesis block
- `'pending'` - Pending state
- `'safe'` - Latest safe block
- `'finalized'` - Latest finalized block
- `'0x...'` - Specific block number (hex)

## Related

- [Block Methods](/jsonrpc-provider/eth-methods/blocks) - All block methods
