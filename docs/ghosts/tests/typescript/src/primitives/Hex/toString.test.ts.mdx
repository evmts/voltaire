---
title: '[TS/JS] src/primitives/Hex/toString.test.ts'
source: 'src/primitives/Hex/toString.test.ts'
---

> Auto-generated from test file: src/primitives/Hex/toString.test.ts

```typescript
import { describe, expect, it } from "vitest";
import {
	InvalidCharacterError,
	InvalidFormatError,
	OddLengthError,
} from "../errors/index.js";
import { fromString } from "./fromString.js";
import type { HexType } from "./HexType.js";
// biome-ignore lint/suspicious/noShadowRestrictedNames: testing the toString method
import { toString } from "./toString.js";

describe("toString", () => {
	it("converts empty hex", () => {
		expect(toString("0x" as HexType)).toBe("");
	});

	it("converts single character", () => {
		expect(toString("0x61" as HexType)).toBe("a");
	});

	it("converts ASCII text", () => {
		expect(toString("0x68656c6c6f" as HexType)).toBe("hello");
		expect(toString("0x48656c6c6f20576f726c6421" as HexType)).toBe(
			"Hello World!",
		);
	});

	it("converts special characters", () => {
		expect(toString("0x40" as HexType)).toBe("@");
		expect(toString("0x313233" as HexType)).toBe("123");
		expect(toString("0x2140232425" as HexType)).toBe("!@#$%");
	});

	it("converts newlines and tabs", () => {
		expect(toString("0x0a" as HexType)).toBe("\n");
		expect(toString("0x09" as HexType)).toBe("\t");
		expect(toString("0x0d0a" as HexType)).toBe("\r\n");
	});

	it("handles uppercase hex", () => {
		expect(toString("0x68656C6C6F" as HexType)).toBe("hello");
		expect(toString("0x48454C4C4F" as HexType)).toBe("HELLO");
	});

	it("handles mixed case hex", () => {
		expect(toString("0x68656c6C6f" as HexType)).toBe("hello");
	});

	it("round-trip with fromString", () => {
		const original = "Hello, World! 123";
		const hex = fromString(original) as HexType;
		const decoded = toString(hex);
		expect(decoded).toBe(original);
	});

	it("throws on missing 0x prefix", () => {
		expect(() => toString("68656c6c6f" as HexType)).toThrow(InvalidFormatError);
	});

	it("throws on odd length", () => {
		expect(() => toString("0x1" as HexType)).toThrow(OddLengthError);
		expect(() => toString("0x123" as HexType)).toThrow(OddLengthError);
	});

	it("throws on invalid hex character", () => {
		expect(() => toString("0x68656c6c6g" as HexType)).toThrow(
			InvalidCharacterError,
		);
		expect(() => toString("0x6865 6c6c6f" as HexType)).toThrow(OddLengthError);
	});

	it("handles long strings", () => {
		const original = "a".repeat(1000);
		const hex = fromString(original) as HexType;
		const decoded = toString(hex);
		expect(decoded).toBe(original);
	});

	it("handles UTF-8 encoded characters", () => {
		const hex = fromString("🚀") as HexType;
		const decoded = toString(hex);
		expect(decoded).toBe("🚀");
	});
});

```
