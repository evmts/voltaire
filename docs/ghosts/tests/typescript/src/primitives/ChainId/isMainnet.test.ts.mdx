---
title: '[TS/JS] src/primitives/ChainId/isMainnet.test.ts'
source: 'src/primitives/ChainId/isMainnet.test.ts'
---

> Auto-generated from test file: src/primitives/ChainId/isMainnet.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as ChainId from "./index.js";

describe("ChainId.isMainnet", () => {
	describe("mainnet chain ID", () => {
		it("returns true for value 1", () => {
			const chainId = ChainId.from(1);
			expect(ChainId.isMainnet(chainId)).toBe(true);
		});

		it("returns true for MAINNET constant", () => {
			const chainId = ChainId.from(ChainId.MAINNET);
			expect(ChainId.isMainnet(chainId)).toBe(true);
		});
	});

	describe("non-mainnet chain IDs", () => {
		it("returns false for Sepolia", () => {
			const chainId = ChainId.from(ChainId.SEPOLIA);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for Goerli", () => {
			const chainId = ChainId.from(ChainId.GOERLI);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for Optimism", () => {
			const chainId = ChainId.from(ChainId.OPTIMISM);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for Arbitrum", () => {
			const chainId = ChainId.from(ChainId.ARBITRUM);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for Base", () => {
			const chainId = ChainId.from(ChainId.BASE);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for Polygon", () => {
			const chainId = ChainId.from(ChainId.POLYGON);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for zero", () => {
			const chainId = ChainId.from(0);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});

		it("returns false for custom chain ID", () => {
			const chainId = ChainId.from(31337);
			expect(ChainId.isMainnet(chainId)).toBe(false);
		});
	});
});

```
