---
title: '[TS/JS] src/primitives/ValidatorIndex/ValidatorIndex.test.ts'
source: 'src/primitives/ValidatorIndex/ValidatorIndex.test.ts'
---

> Auto-generated from test file: src/primitives/ValidatorIndex/ValidatorIndex.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as ValidatorIndex from "./index.js";

describe("ValidatorIndex", () => {
	describe("from", () => {
		it("creates ValidatorIndex from number", () => {
			const idx = ValidatorIndex.from(123456);
			expect(idx).toBe(123456);
		});

		it("creates ValidatorIndex from bigint", () => {
			const idx = ValidatorIndex.from(123456n);
			expect(idx).toBe(123456);
		});

		it("creates ValidatorIndex from hex string", () => {
			const idx = ValidatorIndex.from("0x1e240");
			expect(idx).toBe(123456);
		});

		it("creates ValidatorIndex from decimal string", () => {
			const idx = ValidatorIndex.from("123456");
			expect(idx).toBe(123456);
		});

		it("rejects negative number", () => {
			expect(() => ValidatorIndex.from(-1)).toThrow("cannot be negative");
		});

		it("rejects non-integer number", () => {
			expect(() => ValidatorIndex.from(1.5)).toThrow("safe integer");
		});

		it("rejects value exceeding MAX_SAFE_INTEGER", () => {
			expect(() =>
				ValidatorIndex.from(BigInt(Number.MAX_SAFE_INTEGER) + 1n),
			).toThrow("safe integer");
		});
	});

	describe("toNumber", () => {
		it("converts to number", () => {
			const idx = ValidatorIndex.from(123456);
			expect(ValidatorIndex.toNumber(idx)).toBe(123456);
		});
	});

	describe("equals", () => {
		it("returns true for equal indices", () => {
			const a = ValidatorIndex.from(123456);
			const b = ValidatorIndex.from(123456);
			expect(ValidatorIndex.equals(a, b)).toBe(true);
		});

		it("returns false for unequal indices", () => {
			const a = ValidatorIndex.from(123456);
			const b = ValidatorIndex.from(123457);
			expect(ValidatorIndex.equals(a, b)).toBe(false);
		});
	});
});

```
