---
title: '[TS/JS] src/primitives/Gas/gasLimitFrom.test.ts'
source: 'src/primitives/Gas/gasLimitFrom.test.ts'
---

> Auto-generated from test file: src/primitives/Gas/gasLimitFrom.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { SIMPLE_TRANSFER } from "./gasLimitConstants.js";
import { gasLimitFrom } from "./gasLimitFrom.js";

describe("gasLimitFrom", () => {
	it("creates gas limit from number", () => {
		const limit = gasLimitFrom(21000);
		expect(typeof limit).toBe("bigint");
		expect(limit).toBe(21000n);
	});

	it("creates gas limit from bigint", () => {
		const limit = gasLimitFrom(21000n);
		expect(typeof limit).toBe("bigint");
		expect(limit).toBe(21000n);
	});

	it("creates gas limit from hex", () => {
		const limit = gasLimitFrom("0x5208");
		expect(typeof limit).toBe("bigint");
		expect(limit).toBe(21000n);
	});

	it("uses predefined constants", () => {
		expect(typeof SIMPLE_TRANSFER).toBe("bigint");
		expect(SIMPLE_TRANSFER).toBe(21000n);
	});
});

```
