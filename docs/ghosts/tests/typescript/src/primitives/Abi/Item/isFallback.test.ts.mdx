---
title: '[TS/JS] src/primitives/Abi/Item/isFallback.test.ts'
source: 'src/primitives/Abi/Item/isFallback.test.ts'
---

> Auto-generated from test file: src/primitives/Abi/Item/isFallback.test.ts

```typescript
/**
 * Unit tests for isFallback type guard
 */

import { describe, expect, it } from "vitest";
import { isFallback } from "./isFallback.js";

describe("isFallback", () => {
	it("returns true for fallback items", () => {
		const item = {
			type: "fallback",
			stateMutability: "payable",
		} as const;

		expect(isFallback(item)).toBe(true);
	});

	it("returns false for function items", () => {
		const item = {
			type: "function",
			name: "transfer",
			stateMutability: "nonpayable",
			inputs: [],
			outputs: [],
		} as const;

		expect(isFallback(item)).toBe(false);
	});

	it("returns false for event items", () => {
		const item = {
			type: "event",
			name: "Transfer",
			inputs: [],
		} as const;

		expect(isFallback(item)).toBe(false);
	});

	it("returns false for error items", () => {
		const item = {
			type: "error",
			name: "Unauthorized",
			inputs: [],
		} as const;

		expect(isFallback(item)).toBe(false);
	});

	it("returns false for constructor items", () => {
		const item = {
			type: "constructor",
			stateMutability: "nonpayable",
			inputs: [],
		} as const;

		expect(isFallback(item)).toBe(false);
	});

	it("returns false for receive items", () => {
		const item = {
			type: "receive",
			stateMutability: "payable",
		} as const;

		expect(isFallback(item)).toBe(false);
	});

	it("works with nonpayable fallback", () => {
		const item = {
			type: "fallback",
			stateMutability: "nonpayable",
		} as const;

		expect(isFallback(item)).toBe(true);
	});

	it("works with payable fallback", () => {
		const item = {
			type: "fallback",
			stateMutability: "payable",
		} as const;

		expect(isFallback(item)).toBe(true);
	});
});

```
