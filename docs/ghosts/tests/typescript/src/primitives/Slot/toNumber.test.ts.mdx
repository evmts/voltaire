---
title: '[TS/JS] src/primitives/Slot/toNumber.test.ts'
source: 'src/primitives/Slot/toNumber.test.ts'
---

> Auto-generated from test file: src/primitives/Slot/toNumber.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as Slot from "./index.js";

describe("Slot.toNumber", () => {
	describe("conversion", () => {
		it("converts to number", () => {
			const slot = Slot.from(1000000n);
			expect(Slot.toNumber(slot)).toBe(1000000);
		});

		it("converts zero", () => {
			const slot = Slot.from(0);
			expect(Slot.toNumber(slot)).toBe(0);
		});

		it("converts small value", () => {
			const slot = Slot.from(42);
			expect(Slot.toNumber(slot)).toBe(42);
		});

		it("converts max safe integer", () => {
			const slot = Slot.from(Number.MAX_SAFE_INTEGER);
			expect(Slot.toNumber(slot)).toBe(9007199254740991);
		});

		it("converts from string input", () => {
			const slot = Slot.from("12345");
			expect(Slot.toNumber(slot)).toBe(12345);
		});
	});

	describe("round-trip", () => {
		it("preserves value through from and toNumber", () => {
			const original = 1234567;
			const slot = Slot.from(original);
			const result = Slot.toNumber(slot);
			expect(result).toBe(original);
		});

		it("preserves zero", () => {
			const original = 0;
			const slot = Slot.from(original);
			const result = Slot.toNumber(slot);
			expect(result).toBe(original);
		});
	});

	describe("error cases", () => {
		it("throws on value exceeding MAX_SAFE_INTEGER", () => {
			const slot = Slot.from(BigInt(Number.MAX_SAFE_INTEGER) + 1n);
			expect(() => Slot.toNumber(slot)).toThrow("exceeds MAX_SAFE_INTEGER");
		});

		it("throws on very large value", () => {
			const slot = Slot.from(2n ** 100n);
			expect(() => Slot.toNumber(slot)).toThrow("exceeds MAX_SAFE_INTEGER");
		});
	});

	describe("type consistency", () => {
		it("returns number type", () => {
			const slot = Slot.from(100);
			const result = Slot.toNumber(slot);
			expect(typeof result).toBe("number");
		});
	});
});

```
