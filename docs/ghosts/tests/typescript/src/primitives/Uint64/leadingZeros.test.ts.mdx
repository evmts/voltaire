---
title: '[TS/JS] src/primitives/Uint64/leadingZeros.test.ts'
source: 'src/primitives/Uint64/leadingZeros.test.ts'
---

> Auto-generated from test file: src/primitives/Uint64/leadingZeros.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { MAX, ONE, ZERO } from "./constants.js";
import { from } from "./from.js";
import { leadingZeros } from "./leadingZeros.js";

describe("Uint64.leadingZeros", () => {
	describe("known values", () => {
		it("0 has 64 leading zeros", () => {
			expect(leadingZeros(ZERO)).toBe(64);
		});

		it("1 has 63 leading zeros", () => {
			expect(leadingZeros(ONE)).toBe(63);
		});

		it("2 has 62 leading zeros", () => {
			expect(leadingZeros(from(2n))).toBe(62);
		});

		it("3 has 62 leading zeros", () => {
			expect(leadingZeros(from(3n))).toBe(62);
		});

		it("4 has 61 leading zeros", () => {
			expect(leadingZeros(from(4n))).toBe(61);
		});

		it("255 has 56 leading zeros", () => {
			expect(leadingZeros(from(255n))).toBe(56);
		});

		it("256 has 55 leading zeros", () => {
			expect(leadingZeros(from(256n))).toBe(55);
		});
	});

	describe("powers of 2", () => {
		it("2^10 has 53 leading zeros", () => {
			expect(leadingZeros(from(1n << 10n))).toBe(53);
		});

		it("2^32 has 31 leading zeros", () => {
			expect(leadingZeros(from(1n << 32n))).toBe(31);
		});

		it("2^63 has 0 leading zeros", () => {
			expect(leadingZeros(from(1n << 63n))).toBe(0);
		});
	});

	describe("edge cases", () => {
		it("MAX has 0 leading zeros", () => {
			expect(leadingZeros(MAX)).toBe(0);
		});

		it("MAX >> 1 has 1 leading zero", () => {
			expect(leadingZeros(from(MAX >> 1n))).toBe(1);
		});

		it("0xff has 56 leading zeros", () => {
			expect(leadingZeros(from(0xffn))).toBe(56);
		});
	});
});

```
