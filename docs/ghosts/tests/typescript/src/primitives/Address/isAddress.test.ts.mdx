---
title: '[TS/JS] src/primitives/Address/isAddress.test.ts'
source: 'src/primitives/Address/isAddress.test.ts'
---

> Auto-generated from test file: src/primitives/Address/isAddress.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { Address } from "../index.js";
import { isAddress } from "./isAddress.js";

describe("isAddress", () => {
	it("returns true for valid Address", () => {
		const addr = Address.fromHex("0x742d35cc6634c0532925a3b844bc9e7595f251e3");
		expect(isAddress(addr)).toBe(true);
	});

	it("returns true for zero address", () => {
		const addr = Address.zero();
		expect(isAddress(addr)).toBe(true);
	});

	it("returns true for plain 20-byte Uint8Array", () => {
		expect(isAddress(new Uint8Array(20))).toBe(true);
	});

	it("returns false for wrong length Uint8Array", () => {
		expect(isAddress(new Uint8Array(19))).toBe(false);
		expect(isAddress(new Uint8Array(21))).toBe(false);
		expect(isAddress(new Uint8Array(32))).toBe(false);
	});

	it("returns false for empty array", () => {
		expect(isAddress(new Uint8Array(0))).toBe(false);
	});

	it("returns false for string", () => {
		expect(isAddress("0x742d35cc6634c0532925a3b844bc9e7595f251e3")).toBe(false);
	});

	it("returns false for number", () => {
		expect(isAddress(42)).toBe(false);
		expect(isAddress(0)).toBe(false);
	});

	it("returns false for bigint", () => {
		expect(isAddress(42n)).toBe(false);
	});

	it("returns false for null", () => {
		expect(isAddress(null)).toBe(false);
	});

	it("returns false for undefined", () => {
		expect(isAddress(undefined)).toBe(false);
	});

	it("returns false for object", () => {
		expect(isAddress({})).toBe(false);
		expect(isAddress({ length: 20 })).toBe(false);
	});

	it("returns false for regular array", () => {
		expect(isAddress(new Array(20).fill(0))).toBe(false);
	});

	it("can be used as type guard", () => {
		const value: unknown = Address.fromHex(
			"0x742d35cc6634c0532925a3b844bc9e7595f251e3",
		);
		if (isAddress(value)) {
			// TypeScript now knows value is AddressType
			expect(value.length).toBe(20);
		}
	});
});

```
