---
title: '[TS/JS] src/primitives/PendingTransactionFilter/PendingTransactionFilter.test.ts'
source: 'src/primitives/PendingTransactionFilter/PendingTransactionFilter.test.ts'
---

> Auto-generated from test file: src/primitives/PendingTransactionFilter/PendingTransactionFilter.test.ts

```typescript
import { describe, expect, it } from "vitest";
import * as FilterId from "../FilterId/index.js";
import * as PendingTransactionFilter from "./index.js";

describe("PendingTransactionFilter", () => {
	describe("from", () => {
		it("creates PendingTransactionFilter from filter ID", () => {
			const filterId = FilterId.from("0x1");
			const filter = PendingTransactionFilter.from(filterId);
			expect(filter.filterId).toBe(filterId);
			expect(filter.type).toBe("pendingTransaction");
		});

		it("creates multiple distinct filters", () => {
			const id1 = FilterId.from("0x1");
			const id2 = FilterId.from("0x2");
			const filter1 = PendingTransactionFilter.from(id1);
			const filter2 = PendingTransactionFilter.from(id2);
			expect(filter1.filterId).toBe(id1);
			expect(filter2.filterId).toBe(id2);
			expect(filter1.filterId).not.toBe(filter2.filterId);
		});
	});
});

```
