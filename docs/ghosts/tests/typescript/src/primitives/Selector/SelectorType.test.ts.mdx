---
title: '[TS/JS] src/primitives/Selector/SelectorType.test.ts'
source: 'src/primitives/Selector/SelectorType.test.ts'
---

> Auto-generated from test file: src/primitives/Selector/SelectorType.test.ts

```typescript
import { describe, it } from "vitest";
import type { SelectorType } from "./SelectorType.js";

type Equals<X, Y> = (<T>() => T extends X ? 1 : 2) extends <T>() => T extends Y
	? 1
	: 2
	? true
	: false;

describe("SelectorType", () => {
	it("should be a branded Uint8Array type", () => {
		// biome-ignore lint/suspicious/noExplicitAny: type-level test requires any cast
		const _typeTest = null as any as Equals<SelectorType, Uint8Array>;
		const _assertion: Equals<typeof _typeTest, false> = true;
		_assertion;
	});

	it("should have length 4", () => {
		const selector = new Uint8Array(4) as SelectorType;
		const _length: 4 = selector.length;
		_length;
	});

	it("should not accept plain Uint8Array", () => {
		const plainArray = new Uint8Array(4);
		// @ts-expect-error - plain Uint8Array is not SelectorType
		const _selector: SelectorType = plainArray;
		_selector;
	});

	it("should not accept Uint8Array with wrong length", () => {
		const wrongLength = new Uint8Array(8) as SelectorType;
		// @ts-expect-error - length must be 4
		const _length: 4 = wrongLength.length;
		_length;
	});

	it("should not accept string", () => {
		// @ts-expect-error - string is not SelectorType
		const _selector: SelectorType = "0xa9059cbb";
		_selector;
	});

	it("should not accept number", () => {
		// @ts-expect-error - number is not SelectorType
		const _selector: SelectorType = 0xa9059cbb;
		_selector;
	});

	it("should accept result from from constructor", () => {
		const selector = new Uint8Array(4) as SelectorType;
		const _result: SelectorType = selector;
		_result;
	});
});

```
