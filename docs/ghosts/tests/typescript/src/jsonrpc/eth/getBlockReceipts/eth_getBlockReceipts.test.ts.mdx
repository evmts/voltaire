---
title: '[TS/JS] src/jsonrpc/eth/getBlockReceipts/eth_getBlockReceipts.test.ts'
source: 'src/jsonrpc/eth/getBlockReceipts/eth_getBlockReceipts.test.ts'
---

> Auto-generated from test file: src/jsonrpc/eth/getBlockReceipts/eth_getBlockReceipts.test.ts

```typescript
import { describe, expect, test } from "vitest";
import { GetBlockReceiptsRequest, method } from "./eth_getBlockReceipts.js";

describe("eth_getBlockReceipts", () => {
	describe("Request Creation", () => {
		test("creates request with block number", () => {
			const blockNumber = "0x1234";
			const req = GetBlockReceiptsRequest(blockNumber);
			expect(req).toEqual({
				method: "eth_getBlockReceipts",
				params: [blockNumber],
			});
		});

		test("creates request with latest tag", () => {
			const req = GetBlockReceiptsRequest("latest");
			expect(req).toEqual({
				method: "eth_getBlockReceipts",
				params: ["latest"],
			});
		});

		test("creates request with earliest tag", () => {
			const req = GetBlockReceiptsRequest("earliest");
			expect(req).toEqual({
				method: "eth_getBlockReceipts",
				params: ["earliest"],
			});
		});

		test("method constant is correct", () => {
			expect(method).toBe("eth_getBlockReceipts");
		});
	});

	describe("Request Structure", () => {
		test("returns RequestArguments type with params", () => {
			const req = GetBlockReceiptsRequest("0x1234");
			expect(req).toHaveProperty("method");
			expect(req).toHaveProperty("params");
			expect(Array.isArray(req.params)).toBe(true);
			expect(req.params).toHaveLength(1);
		});

		test("method matches constant", () => {
			const req = GetBlockReceiptsRequest("0x1234");
			expect(req.method).toBe(method);
		});
	});

	describe("Edge Cases", () => {
		test("handles pending tag", () => {
			const req = GetBlockReceiptsRequest("pending");
			expect(req.params?.[0]).toBe("pending");
		});

		test("handles finalized tag", () => {
			const req = GetBlockReceiptsRequest("finalized");
			expect(req.params?.[0]).toBe("finalized");
		});

		test("handles safe tag", () => {
			const req = GetBlockReceiptsRequest("safe");
			expect(req.params?.[0]).toBe("safe");
		});

		test("handles block hash", () => {
			const blockHash =
				"0xd5f1812548be429cbdc6376b29611fc49e06f1359758c4ceaaa3b393e2239f9c";
			const req = GetBlockReceiptsRequest(blockHash);
			expect(req.params?.[0]).toBe(blockHash);
		});
	});
});

```
