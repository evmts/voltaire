---
title: '[TS/JS] src/jsonrpc/JsonRpcVersion/JsonRpcVersion.test.ts'
source: 'src/jsonrpc/JsonRpcVersion/JsonRpcVersion.test.ts'
---

> Auto-generated from test file: src/jsonrpc/JsonRpcVersion/JsonRpcVersion.test.ts

```typescript
import { describe, expect, it } from "vitest";
import { JsonRpcVersion, VERSION, from } from "./index.js";

describe("JsonRpcVersion", () => {
	describe("from", () => {
		it("returns version 2.0", () => {
			expect(from()).toBe("2.0");
		});

		it("matches VERSION constant", () => {
			expect(from()).toBe(VERSION);
		});
	});

	describe("constants", () => {
		it("VERSION is 2.0", () => {
			expect(VERSION).toBe("2.0");
		});

		it("JsonRpcVersion.VERSION is 2.0", () => {
			expect(JsonRpcVersion.VERSION).toBe("2.0");
		});
	});

	describe("namespace", () => {
		it("JsonRpcVersion.from() returns 2.0", () => {
			expect(JsonRpcVersion.from()).toBe("2.0");
		});
	});
});

```
