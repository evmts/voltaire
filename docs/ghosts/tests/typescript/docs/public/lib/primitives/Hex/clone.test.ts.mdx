---
title: '[TS/JS] docs/public/lib/primitives/Hex/clone.test.ts'
source: 'docs/public/lib/primitives/Hex/clone.test.ts'
---

> Auto-generated from test file: docs/public/lib/primitives/Hex/clone.test.ts

```typescript
import { describe, expect, it } from "vitest";
import type { HexType } from "./HexType.js";
import { clone } from "./clone.js";
import { equals } from "./equals.js";
import { from } from "./from.js";

describe("clone", () => {
	it("creates a copy of hex string", () => {
		const hex1 = from("0x1234") as HexType;
		const hex2 = clone(hex1);

		expect(equals(hex1, hex2)).toBe(true);
		expect(hex1).toBe(hex2); // strings are immutable in JS
	});

	it("clones empty hex", () => {
		const hex1 = from("0x") as HexType;
		const hex2 = clone(hex1);

		expect(hex2).toBe("0x");
		expect(equals(hex1, hex2)).toBe(true);
	});

	it("clones long hex values", () => {
		const hex1 = from(`0x${"ab".repeat(100)}`) as HexType;
		const hex2 = clone(hex1);

		expect(equals(hex1, hex2)).toBe(true);
	});
});

```
