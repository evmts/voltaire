---
title: '[TS/JS] docs/primitives/denomination/index.test.ts'
source: 'docs/primitives/denomination/index.test.ts'
---

> Auto-generated from test file: docs/primitives/denomination/index.test.ts

```typescript
/**
 * Tests for docs/primitives/denomination/index.mdx
 *
 * Validates that all code examples in the Denomination documentation work correctly.
 * Denomination provides type-safe Wei, Gwei, and Ether types with compile-time unit tracking.
 */
import { describe, expect, it } from "vitest";

describe("Denomination Documentation - index.mdx", () => {
	describe("Quick Start - Direct Usage", () => {
		it("should create Wei value from bigint", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000n); // 1 Gwei in Wei
			expect(wei).toBe(1_000_000_000n);
		});

		it("should create Gwei value from bigint", async () => {
			const { Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(21n); // 21 Gwei
			expect(gwei).toBe(21n);
		});

		it("should create Ether value from bigint", async () => {
			const { Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n); // 1 Ether (in wei representation)
			expect(ether).toBeDefined();
		});
	});

	describe("Conversions - Wei to Gwei", () => {
		it("should convert Wei to Gwei", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(21_000_000_000n); // 21 Gwei in Wei
			const gwei = Wei.toGwei(wei);
			expect(gwei).toBe(21n);
		});

		it("should convert Gwei to Wei", async () => {
			const { Gwei, Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(21n);
			const wei = Wei.fromGwei(gwei);
			expect(wei).toBe(21_000_000_000n);
		});
	});

	describe("Conversions - Wei to Ether", () => {
		it("should convert Wei to Ether", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000_000_000_000n); // 1 ETH in Wei
			const ether = Wei.toEther(wei);
			expect(ether).toBe(1n);
		});

		it("should convert Ether to Wei", async () => {
			const { Ether, Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const wei = Wei.fromEther(ether);
			expect(wei).toBe(1_000_000_000_000_000_000n);
		});
	});

	describe("Conversions - Gwei to Ether", () => {
		it("should convert Gwei to Ether", async () => {
			const { Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(1_000_000_000n); // 1 ETH in Gwei
			const ether = Gwei.toEther(gwei);
			expect(ether).toBe(1n);
		});

		it("should convert Ether to Gwei", async () => {
			const { Ether, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const gwei = Gwei.fromEther(ether);
			expect(gwei).toBe(1_000_000_000n);
		});
	});

	describe("Conversion Constants", () => {
		it("should have correct Wei per Gwei (10^9)", async () => {
			const { Wei, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const oneGwei = Gwei(1n);
			const weiPerGwei = Wei.fromGwei(oneGwei);
			expect(weiPerGwei).toBe(1_000_000_000n);
		});

		it("should have correct Wei per Ether (10^18)", async () => {
			const { Wei, Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const oneEther = Ether(1n);
			const weiPerEther = Wei.fromEther(oneEther);
			expect(weiPerEther).toBe(1_000_000_000_000_000_000n);
		});

		it("should have correct Gwei per Ether (10^9)", async () => {
			const { Gwei, Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const oneEther = Ether(1n);
			const gweiPerEther = Gwei.fromEther(oneEther);
			expect(gweiPerEther).toBe(1_000_000_000n);
		});
	});

	describe("Gas Price Calculation Example", () => {
		it("should calculate transaction cost correctly", async () => {
			const { Wei, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			// Gas price in Gwei (common format)
			const gasPriceGwei = Gwei(50n); // 50 Gwei

			// Convert to Wei for calculation
			const gasPriceWei = Wei.fromGwei(gasPriceGwei);

			// Calculate total cost: gasPrice * gasUsed
			const gasUsed = 21_000n;
			const txCostWei = gasPriceWei * gasUsed;

			// Convert to Ether for display
			const txCostEther = Wei.toEther(Wei(txCostWei));

			// 50 Gwei * 21000 = 1,050,000 Gwei = 0.00105 ETH
			// Since we're using integer division, check the wei value
			expect(txCostWei).toBe(50n * 1_000_000_000n * 21_000n);
		});
	});

	describe("Transaction Values Example", () => {
		it("should convert Ether to Wei for transaction value", async () => {
			const { Wei, Ether, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			// User inputs value in Ether
			const valueEther = Ether(1n); // 1 ETH

			// Transaction requires Wei
			const valueWei = Wei.fromEther(valueEther);
			expect(valueWei).toBe(1_000_000_000_000_000_000n);

			// Gas price in Gwei converted to Wei
			const gasPrice = Wei.fromGwei(Gwei(50n));
			expect(gasPrice).toBe(50_000_000_000n);
		});
	});

	describe("Wei Constructor", () => {
		it("should create Wei from bigint", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1000n);
			expect(wei).toBe(1000n);
		});

		it("should create Wei from number", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1000);
			expect(wei).toBe(1000n);
		});

		it("should create Wei from string", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei("1000");
			expect(wei).toBe(1000n);
		});
	});

	describe("Wei Static Methods", () => {
		it("should have from() method", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei.from(1000n);
			expect(wei).toBe(1000n);
		});

		it("should have fromGwei() method", async () => {
			const { Wei, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(1n);
			const wei = Wei.fromGwei(gwei);
			expect(wei).toBe(1_000_000_000n);
		});

		it("should have fromEther() method", async () => {
			const { Wei, Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const wei = Wei.fromEther(ether);
			expect(wei).toBe(1_000_000_000_000_000_000n);
		});

		it("should have toGwei() method", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000n);
			const gwei = Wei.toGwei(wei);
			expect(gwei).toBe(1n);
		});

		it("should have toEther() method", async () => {
			const { Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000_000_000_000n);
			const ether = Wei.toEther(wei);
			expect(ether).toBe(1n);
		});
	});

	describe("Gwei Static Methods", () => {
		it("should have from() method", async () => {
			const { Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei.from(100n);
			expect(gwei).toBe(100n);
		});

		it("should have fromWei() method", async () => {
			const { Gwei, Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000n);
			const gwei = Gwei.fromWei(wei);
			expect(gwei).toBe(1n);
		});

		it("should have fromEther() method", async () => {
			const { Gwei, Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const gwei = Gwei.fromEther(ether);
			expect(gwei).toBe(1_000_000_000n);
		});

		it("should have toWei() method", async () => {
			const { Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(1n);
			const wei = Gwei.toWei(gwei);
			expect(wei).toBe(1_000_000_000n);
		});

		it("should have toEther() method", async () => {
			const { Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(1_000_000_000n);
			const ether = Gwei.toEther(gwei);
			expect(ether).toBe(1n);
		});
	});

	describe("Ether Static Methods", () => {
		it("should have from() method", async () => {
			const { Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether.from(1n);
			expect(ether).toBe(1n);
		});

		it("should have fromWei() method", async () => {
			const { Ether, Wei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const wei = Wei(1_000_000_000_000_000_000n);
			const ether = Ether.fromWei(wei);
			expect(ether).toBe(1n);
		});

		it("should have fromGwei() method", async () => {
			const { Ether, Gwei } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const gwei = Gwei(1_000_000_000n);
			const ether = Ether.fromGwei(gwei);
			expect(ether).toBe(1n);
		});

		it("should have toWei() method", async () => {
			const { Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const wei = Ether.toWei(ether);
			expect(wei).toBe(1_000_000_000_000_000_000n);
		});

		it("should have toGwei() method", async () => {
			const { Ether } = await import(
				"../../../src/primitives/Denomination/index.js"
			);

			const ether = Ether(1n);
			const gwei = Ether.toGwei(ether);
			expect(gwei).toBe(1_000_000_000n);
		});
	});
});

```
